# Estado del proyecto

**Nota de alcance (agregada 2026-07-26 tras un incidente real):** en las
notas de abajo, "desplegado y verificado contra el servidor real" o
"`clasp clone`/`pull` + `diff`" significa únicamente que **el código
pusheado coincide con el código que corre en el servidor**. NO significa
que el esquema real de las hojas de Google Sheets involucradas coincide
con lo que ese código espera — son dos verificaciones distintas. Ver
`GUIA_ANTIALUCINACION_ANTIESPAGUETI_1.md` §A.9 para el detalle del
incidente que motivó esta aclaración.

| Tarea | Estado | Notas |
|---|---|---|
| TESO Tesorería 360 — Workflow de pagos (FASE/ESTADO por partida, drill-down Sociedad→Propuesta→Proveedor, moneda por línea, nota de aplicación FX, reactivación unificada, catálogo de roles CAT_USUARIOS) | TERMINADO | Desplegado a TESO (2026-07-26) y verificado contra el servidor real (clasp clone + diff, no solo el mensaje de "Pushed N files"). `revisor` rechazó 3 veces por hallazgos financieros reales (moneda vacía sumada como MXN, monto autorizado sin validar, sin auditoría de quién/cuándo, chips de moneda ambiguos vs. barra de distribución, camino por defecto de "Autorizar" sin persistir el monto, toggles individuales sin manejo de error) — el usuario autorizó explícitamente continuar más allá del tope de 3, se corrigieron todos y `revisor` aprobó en el siguiente ciclo. `qa` encontró y se corrigió además un bug real de comparación de rol sensible a acentos/mayúsculas (`_normalizarRol`/`_esRol`), con validación de lista agregada en la hoja `CAT_USUARIOS` como defensa adicional. Pendiente explícito fuera de esta tarea (ver `PROMPT_TESORERIA360_PAGOS_1.md` y memoria de sesión): diseño de conciliación bancaria y match de comprobantes — no empezado. Deuda técnica anotada (no bloqueante, señalada por `revisor`): sin validación de rol server-side en `updatePartidaPago`/`bulkUpdatePartidasPago` (solo gateo de UI), consistente con el resto de TESO hoy. |
| TESO Tesorería 360 — Carga de propuesta (importar `PROPUESTA.xlsx` real y crear `PROPUESTAS_PAGO`/`PARTIDAS_PAGO`) | TERMINADO | Desplegado a TESO (2026-07-26) y verificado contra el servidor real (clasp clone + diff). Botón "Cargar propuesta" en `View_Pagos.html` (gateado por rol: Solicitante/Contador, no Tesorería) → lee el .xlsx con SheetJS → `previsualizarPropuestaPago` (nunca escribe) → `confirmarCargaPropuestaPago` (solo con lo confirmado). Mapeos de columnas verificados contra el archivo real y `BD.xlsx` con Python antes de escribir código (no supuestos): filas de subtotal detectadas y excluidas, una sola divisa por línea (multi-divisa = error explícito), sociedad resuelta por `ULTIMOS_DIGITOS` de `CAT_CUENTAS_MAPEO` (nunca inventada), `SOLICITANTE` movido a `PARTIDAS_PAGO` por línea (varía mucho dentro de una misma sociedad), `UUID`/`RFC`/`NUMERO_DOCUMENTO` como campos nuevos separados. Hoja "INTERCIAS" excluida a propósito (otro dominio, sin proveedor/factura). `revisor` rechazó 2 veces por hallazgos financieros reales (importes sin sanear separadores de miles, sin `LockService` para folios concurrentes, sin revalidar monto/moneda/UUID-duplicado al confirmar, monto crudo persistido en vez del normalizado) — todos corregidos, aprobado en el 3er ciclo. `qa` confirmó `LISTO` con 8 casos de prueba. |
| TESO Tesorería 360 — Conciliación bancaria + comprobantes (fases 6-7) | TERMINADO | Desplegado a TESO (2026-07-26) y verificado contra el servidor real (`clasp clone`/`pull` + `diff` byte a byte, no solo el mensaje de "Pushed N files"). Match por PARTIDA individual (1:1 y split 1:N; N:1 fuera de alcance a propósito), gate de saldo `SUM_MOV` como badge informativo NO bloqueante junto al banco, catálogo `CAT_CUENTAS_MAPEO`/`TC` extendido (sin catálogos nuevos), `carga-mov`/`HIST_MOVIMIENTOS` reusado tal cual. Nuevos: columnas `CUENTA_PAGO`/`MONEDA_PAGO_REAL`/`TC_APLICADO` en `PARTIDAS_PAGO` (migración aditiva, capturadas en la pestaña "Aplicado" existente por Tesorería, reusando la columna de autorización que ahí queda vacía); hojas `CONCILIACION_PAGOS` (vínculo partida↔movimiento N:M), `COMPROBANTES_PAGO`+`COMPROBANTES_PAGO_PARTIDAS` (archivo + vínculo editable, un PDF consolidado se vincula a N partidas de un jalón y se puede desvincular sin resubir), `CONFIG_CONCILIACION` (tolerancia de búsqueda editable a mano por finanzas, patrón `CONFIG_DISTRIBUCION`). Conciliar/subir comprobantes: Tesorería y Contador, gateado en frontend y backend. Comprobantes sube a carpeta de Drive fija provista por el usuario. `revisor` rechazó 1 vez por hallazgos reales (bug de UI donde el select de moneda de pago se revertía solo por falta de mutación local antes de `redraw()`, riesgo de perder en silencio un vínculo de conciliación ya confirmado si cambiaba la ventana de búsqueda, falta de gateo de rol en los botones nuevos de la UI, comentario desalineado sobre quién captura los campos) — los 4 corregidos, aprobado en el 2° ciclo. `qa` confirmó `LISTO` trazando 8 casos (split exacto y fuera de tolerancia, conflicto de movimiento ya vinculado a otra partida, migración aditiva sobre hoja poblada, comprobante consolidado + desvinculación parcial, rechazo de rol sin permiso, conversión de moneda consistente entre funciones, flujo UI completo). Bug real propio encontrado y corregido en el propio despliegue: un comentario JSDoc con la secuencia literal `*/` (`CAT_*/CONFIG_*`) cerraba el bloque de comentario antes de tiempo y rompía la sintaxis del `.gs` en `clasp push` — detectado por el error de sintaxis de `clasp`, no por revisor/qa (ninguno ejecuta el código). Pendiente fuera de esta entrega (decidido explícitamente): match N partidas↔1 movimiento consolidado, e identificar el monto/página exacto de cada pago dentro de un PDF de comprobante consolidado — ambos quedan para una iteración futura. |
| TESO Tesorería 360 — Bloque 3: `CUENTA_PAGO` obligatoria antes de marcar "Pagada" en la pestaña Aplicado | TERMINADO | Validación cliente (toggle individual bloqueado + botón "Aplicar pagos" pre-valida toda la selección, mismo patrón que `autorizarSeleccionadas`) y servidor (`_updatePartidaPagoRow` rechaza `pagado:true` sin `CUENTA_PAGO`, ya sea en el mismo patch o ya persistida). Solo bloquea al marcar Pagada, nunca al desmarcar. `revisor` rechazó 1 vez por un candado circular real (el selector de cuenta solo se mostraba cuando la partida YA estaba pagada, dejando el flujo inutilizable para partidas nuevas) — corregido quitando `&& l.pagado` de la condición, aprobado en el 2° ciclo. `qa` `LISTO` con 6 casos trazados (bloqueo individual, captura-luego-pagar, bypass directo al backend, desmarcar sin restricción, bloque mixto bloqueado completo, bloque completo exitoso). Pusheado y verificado byte a byte contra el servidor (`clasp pull`+diff). **Pendiente confirmación del usuario para `clasp deploy -i`.** Primera de 3 tareas relacionadas (ver también Bloque 2: acceso real por `CAT_USUARIOS`, Bloque 1: notificaciones por correo por sociedad — van en rondas separadas). |
| TESO Tesorería 360 — Bloque 2: acceso real bloqueado a quien no esté en `CAT_USUARIOS` con rol activo | TERMINADO | `_tieneAccesoValido(ss)` (mismo criterio "falla abierto si catálogo vacío" del resto del módulo) gatea ahora lectura Y escritura: `getPropuestasPago` (nuevo status `sin_acceso` distinto de `error`, para pantalla dedicada), `updatePartidaPago`, `bulkUpdatePartidasPago`, `setPartidasPagoChecked`, `solicitarReactivacion`, `previsualizarPropuestaPago`, `confirmarCargaPropuestaPago`, `getCatalogoCuentasPago`, `getConciliacionPagos`, `buscarCandidatosMovimiento`, `getComprobantesPago` (`conciliarPartida`/`desconciliarPartida`/`subirComprobante`/`desvincularComprobante` ya tenían un gate más estricto, sin cambios). Bug real encontrado de paso: `ACTIVO` en `CAT_USUARIOS` es texto libre y la comparación anterior (`&& u.activo`) trataba cualquier texto no vacío como activo, incluida la palabra "No" — corregido con `_esActivo()` explícito. Frontend: pantalla dedicada de "Sin acceso" en vez del banner de error genérico. El usuario confirmó que `CAT_USUARIOS` ya tiene gente cargada en producción. `revisor` rechazó 1 vez (4 hallazgos: `VALORES_ACTIVO` sin la forma femenina "activa", campo `tieneAcceso` agregado a `getUsuarioActual` sin usar, `state.sinAcceso` nunca se reseteaba dejando el botón "Reintentar" roto, y 6 funciones del frontend que silenciaban errores del backend con solo `.catch()` sin revisar `res.status`) — los 4 corregidos, aprobado en el 2° ciclo. `qa` `LISTO` con 7 casos trazados (catálogo vacío, texto "Sí"/"No", checkbox booleano, email no registrado, recuperación de acceso sin refrescar, mutación rechazada con pestaña abierta desde antes). Desplegado a producción (`@21`), verificado byte a byte contra el servidor real. |
| TESO Tesorería 360 — Bloque 1: notificaciones por correo por sociedad al cambiar fase/estado | TERMINADO_TEMPORAL (falta prueba real de correo) | Desplegado a producción (`@22`). Pendiente: el usuario configure al menos una sociedad en "Notificaciones de Pagos" con su propio correo y confirme un envío de prueba real (requisito de `.claude/DEFINICION_DE_TERMINADO.md`) antes de dar el bloque por 100% cerrado. |
| TESO Tesorería 360 — Bloque 1b: editar email/nombre inline + alta multi-sociedad en Notificaciones de Pagos | TERMINADO | Solo frontend (`View_NotificacionesPagos.html`), backend sin cambios. `revisor` rechazó 2 veces (ronda 1: 6 hallazgos — encadenado de fallas inconsistente entre error de negocio y excepción real, fallback silencioso a la sociedad vista cuando el usuario destildaba todo el checklist, botón "Agregar" no reactivo al teclear, inputs editables sin `data-focus-key`, función que excedía el límite de líneas, filtro por sociedad duplicado; ronda 2: 1 hallazgo — la clave de foco usaba el índice del array en vez de un identificador estable, podía restaurar el foco en la fila equivocada tras eliminar otra fila) — usó los 3 intentos completos del proyecto, aprobado en el 3°. `qa` `LISTO` con 8 casos trazados. Desplegado a producción (`@23`), verificado byte a byte. | Hoja nueva `CONFIG_NOTIFICACIONES_PAGOS` (SOCIEDAD/EMAIL/NOMBRE/ACTIVO/FECHA_ALTA, patrón `getListaDistribucion`/`guardarListaDistribucion` de `Codigo.js` adaptado con dimensión sociedad — nunca reusa `CONFIG_DISTRIBUCION`, que es global sin sociedad). `_tipoTransicionNotificable` detecta las 6 transiciones notificables (revisado/autorizado/rechazado/liberado/subido/aplicado) con las mismas firmas de patch que `_camposAuditoriaPorFase`. `_notificarTransicionPagos` engachada en `updatePartidaPago`/`bulkUpdatePartidasPago` tras mutación exitosa (SRP: la mutación no sabe de correo), agrupa por sociedad (1 correo por sociedad+transición, no por partida), nunca rompe la transacción si `MailApp` falla (try/catch anidado: uno por sociedad para que una falla no bloquee las demás del mismo lote, uno general de respaldo). Vista nueva `View_NotificacionesPagos.html` (archivo aparte, no tab de `View_Pagos.html`, decidido con el usuario) registrada en el menú de `Shared_Core.html` e `Index_1.html`. Edición de la lista gateada a Tesorería+Contador (`_puedeConciliarOComprobantes` reusado). Hallazgo de `planificador` de paso: `TESO_src/*.html` está desactualizado (documentado en `GUIA_ANTIALUCINACION_ANTIESPAGUETI_1.md` §A.9), la fuente real de los `.html` vive en la raíz del proyecto — confirmado con diff antes de tocar el menú. `revisor` rechazó 1 vez (2 hallazgos reales: `FECHA_ALTA` se pisaba con "ahora" en cada guardado en vez de preservarse por destinatario, y `View_NotificacionesPagos.html` no tenía ningún gateo de rol en el frontend aunque el backend sí lo validaba) — corregidos, aprobado en el 2° ciclo. `qa` `LISTO` con 8 casos trazados (transición simple, bulk misma sociedad, bulk mezclando sociedades, sociedad sin destinatarios, patch no-notificable, falla de `MailApp` aislada por sociedad, guardar una sociedad sin afectar otra, usuario sin permiso solo puede ver no editar). Pusheado y verificado byte a byte contra el servidor (incluye el archivo nuevo, ahora 31 archivos). Desplegado a producción (`@22`). **Pendiente: el usuario configure al menos una sociedad en "Notificaciones de Pagos" con su propio correo y confirme un envío de prueba real** (requisito de `.claude/DEFINICION_DE_TERMINADO.md`) antes de dar el bloque por 100% cerrado. |
| TESO Tesorería 360 — Soporte real de multi-rol por persona en `CAT_USUARIOS` | TERMINADO | Hallazgo real del usuario: tenía 5 filas con el mismo email, una por rol (Solicitante/Contador/Tesorería/Director/Auditor) — el sistema solo usaba la PRIMERA fila que encontraba (`.filter(...)[0]`), así que quedaba atrapado con "Solicitante" sin importar sus otros roles. Backend: `_rolUsuarioActual` (devolvía 1 rol) reemplazado por `_rolesUsuarioActual` (array) + `_tieneAlgunRol(ss, rolesEsperados)`; `_puedeConciliarOComprobantes`, `_tieneAccesoValido`, `solicitarReactivacion` migrados; `getUsuarioActual` ahora devuelve `roles` (array), se retiró `rol` (singular) a propósito para no dejar un alias que reintrodujera el mismo bug. Frontend: `puedeActuar()` (`View_Pagos.html`) y `puedeEditar()` (`View_NotificacionesPagos.html`) migrados a revisar el array completo. `revisor` aprobó en el 1er intento (barrido completo sin referencias residuales, trazó el caso real de 5 roles, confirmó filtro de inactivos intacto). `qa` confirmó `LISTO` con 6 casos trazados (usuario real de 5 roles, 1 solo rol sin regresión, fila inactiva excluida del array, usuario no registrado bloqueado, catálogo vacío sigue fallando abierto, `solicitarReactivacion` con multi-rol). Antes de liberar se confirmó además que el cambio no tocó la lógica de notificaciones por correo (`_notificarTransicionPagos`/`_enviarCorreoNotificacionPago`/`getListaNotificacionesPago`/`guardarListaNotificacionesPago` intactas) ni el flujo de alta/edición de destinatarios en `View_NotificacionesPagos.html` (`guardarParaSociedad`/`guardar`/`agregar` intactos), solo se corrigió el gateo de acceso que los envolvía. Pusheado y verificado byte a byte contra el servidor (`clasp pull` + diff). Desplegado a producción (`@24`). |
| TESO Tesorería 360 — Ronda de 4 pendientes reportados 2026-07-27: (A) rediseño 10x del correo + etiquetas de fase + link al detalle + quién liberó cada fase, (B) monto parcial al autorizar y al aplicar, (C) bug propuesta "Ventura" no aparece en partidas, (D) correo faltante al cargar propuesta | TERMINADO (fila desactualizada — Bloques B/C/D quedaron cada uno con su propia fila `TERMINADO` más abajo, y la re-prueba de integración final de los 5 bloques juntos también está `TERMINADO`/`LISTO PARA PRODUCCIÓN`. Limpieza de bitácora 2026-08-02, sin cambios de código.) | Orden de trabajo elegido por el usuario: del último al primero como los reportó (A→B→C→D). Mapeo de etiquetas del correo confirmado con el usuario (Opción A): carga→CARGADA (reservado, no disparado todavía), revisado(Contador)→CONFIRMADA, autorizado(monto)→AUTORIZADA, liberado(Tesorería a banco)→sin cambio "LIBERADA A BANCO", subido(comprobante)→SUBIDA EN BANCA, aplicado(final)→APLICADA EN BANCA, rechazado→RECHAZADA. Confirmado con el usuario: el renombre es solo texto del correo, NO toca el motor real de FASE/ESTADO ni la UI de Pagos. **Bloque A completado**: `_tipoTransicionNotificable` ahora retorna `etiqueta` por rama (misma función que `verbo`, nunca se desalinea); `_enviarCorreoNotificacionPago` reescrito — marca "Sistema de Pagos" (antes "TESO Tesorería 360"), asunto `PROPUESTA DE PAGOS "SOCIEDAD" FECHA "DD/MM/AAAA" <ETIQUETA>`, bloque de auditoría por partida (`_auditoriaTextoPartida`, muestra solo fases con `*_POR` ya lleno, usando columnas que ya existían en `PARTIDAS_PAGO`), botón "Ver en el sistema" a la URL base vía `ScriptApp.getService().getUrl()` (nunca hardcodeada, con try/catch local que no rompe el envío si falla), rediseño visual con la paleta Stripe-like ya usada en toda la app. Deep-link real a sociedad/partida específica quedó fuera de alcance a propósito (requeriría routing por hash, no existe hoy) — decisión explícita del usuario de dejarlo para un bloque aparte si se necesita. Bug real encontrado de paso (no relacionado a este bloque): `_pagoSheetToObjects` y la firma de `_rolesUsuarioActual` habían desaparecido del archivo local, reemplazadas por un carácter suelto — reconstruido exacto contra la copia verificada del servidor (diff limpio) antes de tocar nada más. `revisor` aprobó en el 1er intento. `qa` confirmó `LISTO` con 7 casos trazados (revisado sin auditoría previa, aplicado con las 5 fases completas, rechazado con auditoría parcial, lote con partidas en distinto punto del historial, sociedad sin destinatarios sin efectos secundarios, `getUrl()` fallando sin romper el envío, formato de monto con decimales/moneda distinta). Ronda extra pedida por el usuario ("check como usuario"): segundo `revisor` con lente de UX/usabilidad real (no solo código) — RECHAZÓ 1 vez por 2 hallazgos bloqueantes: `display:flex`+`gap` en el header del correo no se soporta en Outlook de escritorio (motor Word, el gap se ignora y el pill de fase queda pegado a la fecha) — corregido a `margin-right` inline; y riesgo de truncamiento del asunto en móvil antes de mostrar la etiqueta de fase — el usuario decidió explícitamente dejar el formato de asunto tal cual lo dictó, aceptando ese riesgo. De paso se ajustó el texto del botón de "Ver en el sistema" a "Abrir Sistema de Pagos" (más honesto, ya que no es un deep-link). Además, `qa` extra pedido por el usuario ("revisión de calidad a las solicitudes"): chequeo de regresión completo sobre Propuestas de Pago (`getPropuestasPago`/`updatePartidaPago`/`bulkUpdatePartidasPago`, drill-down y gateo por rol en `View_Pagos.html`) tras la corrupción encontrada y el cambio de multi-rol — `LISTO`, 40/40 funciones invocadas vs. definidas coinciden, sin residuos de la corrupción, flujo completo sano para usuarios de 1 rol y multi-rol. Pusheado y verificado byte a byte contra el servidor (`clasp pull` + diff) con los 2 ajustes de UX incluidos. Desplegado a producción (`@25`). Bloques B→C→D completados después (ver sus propias filas) y re-probados juntos en la fila de "Re-prueba de integración FINAL". El único pendiente real que sigue abierto es el mismo de la fila "Bloque 1" más arriba: probar con un envío real para confirmar visualmente asunto/etiqueta/auditoría/link. |
| TESO Tesorería 360 — Auditoría de workflow completo pedida por el usuario ("check como usuario experto de cada fase") — 3 hallazgos reales PREEXISTENTES, corregidos | TERMINADO | `revisor` en rol de "usuario experto por fase" recorrió `View_Pagos.html`+`Svc_Pagos.js` de punta a punta y encontró: **(1)** `updatePartidaPago`/`bulkUpdatePartidasPago`/`_updatePartidaPagoRow` solo validan `_tieneAccesoValido` (¿tiene ALGÚN rol?), nunca `_tieneAlgunRol` para el rol específico de esa transición ni el `FASE`/`ESTADO` de origen de la partida — el gateo real vive SOLO en el cliente (`puedeActuar()` solo oculta botones), cualquier rol podría en teoría disparar cualquier transición o saltarse el orden de fases llamando el backend directo. Viola `DEFINICION_DE_TERMINADO.md` §Financiero. **(2)** Una partida rechazada y reactivada después de que pasó la fecha de caducidad de su propuesta (`_fechaCaducidad`, sábado siguiente a `FECHA_CARGA`) vuelve a recalcularse como `'caducada'` en la siguiente lectura (`getPropuestasPago`) — y `'caducada'` no es reactivable ni tiene ninguna acción en la UI: callejón sin salida real para un caso de negocio común (reactivar algo que tardó en corregirse). **(3)** Rechazar/reactivar una partida no deja auditoría `POR/FECHA` en `PARTIDAS_PAGO` (no existen columnas `RECHAZADO_POR/FECHA` ni `REACTIVADO_POR/FECHA`) — el único rastro es el pie del correo de esa transición puntual, no algo consultable después desde la hoja. Hallazgos NO bloqueantes / preguntas abiertas para el usuario: `solicitarReactivacion` nunca dispara correo (asimetría con "rechazada", que sí notifica); "Rechazar" solo existe en la pestaña Tesorería, ninguna otra fase; sin toast de confirmación de éxito en ninguna mutación de fase; sin protección de doble-click en botones bulk; sin `LockService` en las mutaciones de partida individual (a diferencia de carga/conciliación/comprobantes, que sí lo usan). Confirmado que SÍ funciona bien: las 6 firmas de `_tipoTransicionNotificable` cubren todas las transiciones reales que dispara el frontend (nada queda mudo), `CUENTA_PAGO` obligatoria antes de pagar está bien bloqueada en cliente y servidor, el modelo multi-rol está sano sin regresión. **Usuario eligió atacarlo YA, antes de Bloques B/C/D.** **Implementado**: (1) `_validarTransicionPermitida(roles, catalogoVacio, faseActual, patch)` — nueva, llamada desde `_updatePartidaPagoRow` antes de cualquier otra validación, repite en servidor exactamente lo que ya exige el frontend por botón (Contador: fase→tesoreria desde `contabilidad`; Tesorería: autorizar desde `tesoreria`, rechazar sin restricción de fase (decisión explícita del usuario), subir desde `banca`, aplicar desde `aplicado`, capturar cuenta/moneda/TC, reactivar; Contador: liberar a banco desde `liberacion2`), falla abierto si `CAT_USUARIOS` vacío, `checked`/`comentario`/`notaAplicacion` quedan sin gate a propósito (igual que el frontend). `updatePartidaPago`/`bulkUpdatePartidasPago`/`setPartidasPagoChecked`/`solicitarReactivacion` calculan `roles`/`catalogoVacio` una sola vez por request (no por partida en bulk) y los pasan. (2) Columna nueva `FECHA_REACTIVACION`: `getPropuestasPago` calcula la caducidad contra esa fecha cuando existe, en vez de siempre `FECHA_CARGA` de la propuesta original — una partida que nunca se rechazó calcula idéntico a antes. (3) Columnas nuevas `RECHAZADO_POR/FECHA`, `REACTIVADO_POR/FECHA` (aditivo, recogido automático por `_migrarColumnasFinalesPartidasPago` ya existente), estampadas por 2 ramas nuevas en `_camposAuditoriaPorFase`. `revisor` rechazó 1 vez (ronda 1: la reactivación instantánea — firma `{estado:'activa', reactivacionSolicitada:false}` — no tenía rama de gate en `_validarTransicionPermitida`, así que cualquier rol válido podía reactivar una partida rechazada llamando el backend directo sin ser Tesorería, dejando además una auditoría que aparentaba una aprobación legítima; y `solicitarReactivacion` no pasaba `roles`/`catalogoVacio` a `_updatePartidaPagoRow`) — corregido agregando la rama de gate y el cálculo en `solicitarReactivacion`, aprobado en el 2° ciclo. `qa` confirmó `LISTO` con 12 casos trazados (rol+fase correctos, rol correcto con fase incorrecta bloqueado, rol incorrecto bloqueado, ataque directo de reactivación sin Tesorería bloqueado, reactivación legítima por Tesorería funciona, solicitud de reactivación por otro rol sin gate, catálogo vacío sigue fallando abierto, multi-rol funciona para ambos roles, caducidad sin reactivación idéntica a antes, caducidad post-reactivación ya no vuelve a atraparse, bulk con partidas mixtas preserva éxito parcial, migración de columnas nuevas verificada). Pusheado y verificado byte a byte contra el servidor (`clasp pull` + diff). Desplegado a producción (`@25`, junto con el Bloque A). |
| TESO Tesorería 360 — Incidente post-despliegue: esquema real de `PROPUESTAS_PAGO`/`PARTIDAS_PAGO` no coincidía con lo que el código (ya verificado) esperaba | TERMINADO (inferido — sin confirmación explícita registrada de que se corrió la migración, pero la app ha operado sin interrupción durante ~30 despliegues posteriores desde entonces, algo que no sería posible si el esquema siguiera desalineado; limpieza de bitácora 2026-08-02) | Al liberar la entrega anterior a producción (`@18`), la app se rompió para todos los usuarios — no por el código nuevo, sino porque las hojas reales tenían un esquema desalineado de una migración de una ronda anterior que nunca se completó (o se revirtió al editar a mano). `PROPUESTAS_PAGO`: la celda F1 decía "CONTADOR" en vez de "NOTA_GENERAL" — corregido a mano por el usuario directamente en Sheets, ya resuelto. `PARTIDAS_PAGO`: faltaban `NUMERO_PROVEEDOR`/`REFERENCIA` y sobraban `ULTIMA_ACCION_POR`/`ULTIMA_ACCION_FECHA` — se escribió una migración automática (`_migrarEsquemaMedioPartidasPago`, detecta la firma exacta del esquema viejo y usa `insertColumnBefore`/`deleteColumns` para reacomodar filas de datos reales sin borrarlas) — **pusheada, pendiente de confirmación del usuario para liberar (`clasp deploy -i`)**. De paso se corrigió un segundo bug real en la migración aditiva de columnas finales (`_migrarColumnasFinalesPartidasPago` dependía de `getLastColumn()`, que Apps Script puede inflar por celdas con solo formato sin contenido — se cambió a comparar el valor real de cada celda). Ver `GUIA_ANTIALUCINACION_ANTIESPAGUETI_1.md` §A.9 (regla nueva agregada por este incidente: código verificado ≠ esquema de datos verificado). |
| TESO Tesorería 360 — Funciones TEMPORALES `limpiarPagos`/`limpiarConciliacionPagos` para resetear datos antes de una ronda de pruebas de workflow completo | TERMINADO (temporal, ver nota) | **IMPORTANTE — recordar retirar antes del deploy final de esta ronda** (decisión explícita del usuario, mismo precedente que `_debugResetHojasPago` — una función que borra datos financieros reales no debe quedar invocable permanentemente en producción). Bloque delimitado con el marcador `TEMPORAL_PRUEBAS_2026-07-27` (inicio/fin) al final de `Svc_Pagos.js`, fácil de borrar completo. `limpiarPagos`: borra `PROPUESTAS_PAGO`+`PARTIDAS_PAGO`+`CONCILIACION_PAGOS`+`COMPROBANTES_PAGO_PARTIDAS` (cascada, sin dejar vínculos huérfanos apuntando a partidas borradas) — NO borra `COMPROBANTES_PAGO` ni archivos de Drive. `limpiarConciliacionPagos`: borra `CONCILIACION_PAGOS`+`COMPROBANTES_PAGO`+`COMPROBANTES_PAGO_PARTIDAS`, sin tocar Propuestas/Partidas. Ambas exigen `payload.confirmar==='ELIMINAR'` (string exacto), gateadas a Tesorería (falla abierto si `CAT_USUARIOS` vacío), `LockService`, y ahora registran en `Logger` quién/cuándo/cuántas filas (hallazgo del revisor, checklist Financiero — ninguna mutación sin rastro de autoría, ni siquiera temporal). No están expuestas en ningún menú/botón — se ejecutan desde el editor de Apps Script (Extensiones → Apps Script → elegir la función → Ejecutar; primera vez pedirá autorizar permisos). `revisor` rechazó 1 vez (sin log de auditoría, duplicación de código entre ambas funciones, una línea residual huérfana de una edición anterior) — corregido extrayendo `_limpiarHojasPagoConSalvaguardas` como helper compartido, aprobado en el 2° ciclo. `qa` confirmó `LISTO` con 8 casos (sin confirmar, confirmar con booleano en vez de string, rol incorrecto, rol correcto, catálogo vacío, hoja ya vacía sin romper el rango, `limpiarConciliacionPagos` nunca toca Propuestas/Partidas, no borra archivos reales de Drive). **RETIRADAS del código antes del deploy final**, tal como se decidió — confirmado con re-prueba de integración (`revisor`) que ya no queda ninguna función real en `Svc_Pagos.js` (solo una nota histórica en comentario), y verificado byte a byte contra el servidor tras el push de retiro. Backup del bloque completo guardado fuera del repo (scratchpad de la sesión) por si se necesita reconstruir para una futura ronda de pruebas. |
| TESO Tesorería 360 — Header con identidad real de Google (antes "Usuario Demo"/"Administrador" fijo) | TERMINADO | Hallazgo real del usuario: "al entrar al sistema no me pide nada" — el header nunca mostraba quién entró de verdad, aunque el backend ya capturaba correctamente `Session.getActiveUser().getEmail()` en toda la auditoría. Confirmado que `appsscript.json` ya tenía la config correcta (`access:"DOMAIN"` + `executeAs:"USER_ACCESSING"`, Google ya autentica de verdad, solo faltaba mostrarlo en pantalla). `Index_1.html`: los 2 `<p>` del header ganaron `id="veva-user-name"`/`id="veva-user-role"` (texto inicial "Cargando…"). `Shared_Core.html`: `vevaSetupHeader()` ahora llama `vevaCargarUsuarioHeader()`, que usa `getUsuarioActual()` (ya existente en `Svc_Pagos.js`, función global sin gate — cualquier usuario del dominio recibe su email real, esté o no en `CAT_USUARIOS`) para mostrar el email real + rol(es) (o "Sin rol asignado"). `revisor` rechazó 1 vez (`.catch()` completamente silencioso, sin `console.error`, y el header quedaba colgado en "Cargando…" para siempre si la llamada fallaba — violaba `DEFINICION_DE_TERMINADO.md` §Genérico) — corregido con `console.error` + texto "Sesión no identificada" en ambas ramas de falla, aprobado en el 2° ciclo. `qa` confirmó `LISTO` con 8 casos (1 rol, multi-rol, usuario no registrado en `CAT_USUARIOS` sigue viendo su email, falla de backend, falla de red, no bloquea sidebar/router, persiste al navegar entre vistas, `getUsuarioActual` no tiene gate Pagos-específico). Pusheado y verificado byte a byte contra el servidor. Desplegado a producción (`@26`). |
| TESO Tesorería 360 — Re-prueba de integración completa como "usuario experto por fase" antes del deploy final de la ronda 2026-07-27 | TERMINADO | Pedida explícitamente por el usuario tras completar Bloques A+Auditoría+funciones temporales+header: recorrido de punta a punta del ciclo de vida de una partida (carga→revisar→autorizar→liberar→subir→aplicar, más rechazo/reactivación), verificando que TODOS los cambios de la ronda funcionan bien JUNTOS (no solo cada uno por separado). 7 casos de integración trazados: ciclo feliz completo (gate rol+fase nuevo no bloquea a los roles correctos en cada paso, correo con etiqueta correcta en cada transición, auditoría del correo solo muestra fases ya completadas), rechazo+reactivación con el fix de caducidad (ya no vuelve a atraparse), usuario multi-rol ejecutando pasos de ambos roles sobre la misma partida, header vs. auditoría (misma fuente `Session.getActiveUser().getEmail()`, sin riesgo de desincronización), migración aditiva de las 5 columnas nuevas sin pérdida de datos ni corrimiento de columnas existentes, correo disparado SOLO tras aprobar el gate nuevo (nunca antes, nunca si rechazó), `bulkUpdatePartidasPago` con lote mixto solo notifica las partidas que sí pasaron. Único hallazgo (procedimental, no bug funcional): las funciones temporales de limpieza seguían en el código — ya estaban en la cola para retirarse antes del deploy final, se hizo de inmediato (ver fila de `limpiarPagos`/`limpiarConciliacionPagos`). Con eso resuelto: `LISTO PARA PRODUCCIÓN`. Desplegada la ronda completa a `@26` (Bloque A + Bloque Auditoría ya estaban en `@25`; header + retiro de funciones temporales quedaron en `@26`). |
| TESO Tesorería 360 — Bloque B: pago parcial al aplicar + integración con conciliación bancaria | TERMINADO | Pedido original: "cuando autoriza tesorería o aplica el pago la opción de parcial". Diagnóstico: autorizar parcial ya existía completo (`APPROVAL_TYPE`/`MONTO_AUTORIZADO`), solo faltaba "aplicar parcial". 2 decisiones del usuario: (1) sub-hoja nueva `APLICACIONES_PAGO` (auditoría completa por evento, no un campo acumulado sin historial — mismo patrón que `CONCILIACION_PAGOS`); (2) la conciliación bancaria debe ver cada liberación parcial de inmediato, sin esperar el 100%. **Implementado**: hoja `APLICACIONES_PAGO` (`ID/PARTIDA_ID/MONTO/APLICADO_POR/APLICADO_FECHA`); helper `_montoEfectivoPartida(l)` que de paso corrigió un bug real preexistente (3 funciones de conciliación calculaban el objetivo contra el monto nominal, sin respetar autorización parcial); función nueva `aplicarPagoParcial` (con `LockService`, valida rol/fase/estado/cuenta/monto/saldo, inserta en `APLICACIONES_PAGO`, y solo si el acumulado cubre el 100% cierra la partida vía `_updatePartidaPagoRow` y dispara el correo — nunca por cada aplicación parcial); `pagado` pasó a ser un estado 100% derivado, ya no forzable directo (`_updatePartidaPagoRow` lo rechaza si el acumulado no alcanza). `getConciliacionPagos`/`buscarCandidatosMovimiento`/`conciliarPartida` migrados al acumulado vigente como objetivo (la conciliación ya no espera el 100%); la ventana de búsqueda de movimientos ahora usa el rango de fechas de `APLICACIONES_PAGO`, no `APLICADO_FECHA` (que se estampa solo al completar). **Hallazgo adicional del usuario durante el diseño, corregido de paso**: `_validarTransicionPermitida` (Bloque Auditoría) solo revisaba FASE, nunca ESTADO — una partida rechazada conserva su última FASE (nunca se pisa al rechazar), así que en teoría podía seguir "avanzando" vía llamada directa al backend. Se agregó un candado general `estadoActual==='activa'` en las 8 transiciones de avance, y `estadoActual==='rechazada'` para la reactivación — con esto "Cancelar" garantiza de verdad que no salga más dinero, sin necesidad de bloquear el botón de rechazo en sí. UI: pestaña Aplicado ya no tiene el toggle libre de "Pagada" — muestra progreso "$X de $Y" + input/botón "Aplicar" (Tesorería, con cuenta ya capturada); "Aplicar pagos" (bulk) aplica el saldo pendiente de cada seleccionada vía `aplicarPagoParcial`. `revisor` rechazó 1 vez (falta gate de `estadoActual` dentro de `aplicarPagoParcial` mismo — una aplicación parcial que no completa el 100% nunca pasa por `_updatePartidaPagoRow`, así que el candado tenía que vivir también ahí — y `solicitarReactivacion` no pasaba `roles`/`catalogoVacio` a la nueva firma) corregido, aprobado 2° ciclo; 2ª ronda (tras el candado ya integrado) rechazó por duplicación DRY (`acumuladoPorPartida` repetido en 2 funciones, extraído a `_acumuladoAplicadoPorPartida`) y ambigüedad de UI en conciliación (el acumulado se mostraba sin distinguirlo del total, corregido a "$X de $Y") — aprobado 2° ciclo. `qa` confirmó `LISTO` con 11 casos (ciclo completo parcial→completo, cierre real dispara correo una sola vez, exceder saldo rechazado, ataque directo a partida rechazada bloqueado por el candado nuevo, reactivación sigue funcionando, `aplicarPagoParcial` sobre partida rechazada bloqueado sin llegar a completar, conciliación ve el pago parcial de inmediato, vínculo ya confirmado no desaparece al crecer el objetivo, multi-rol, catálogo vacío, bulk mixto). Deuda técnica aceptada explícitamente (cosmética, no bloqueante): la fecha mostrada en la fila de conciliación sigue leyendo `APLICADO_FECHA` de `PARTIDAS_PAGO` (vacío hasta el 100%) en vez de la fecha de la aplicación más reciente. Pusheado y verificado byte a byte contra el servidor (`clasp pull` + diff). Desplegado a producción (`@27`). |
| TESO Tesorería 360 — Bloque C: bug — propuesta cargada ("Ventura") no aparecía en ningún lado | TERMINADO | Investigado con el usuario paso a paso (no especulación): preview salió limpio, confirmó normal, backend reportó éxito — pero SÍ tenía un filtro de OTRA sociedad activo (`state.selectedSociedad`) de una navegación anterior. Se leyó `confirmarCargaPropuestaPago` en el backend y se confirmó que no hay ningún bug de datos ahí (PROPUESTA_ID/PARTIDA_ID quedan bien vinculados) — el problema era 100% de UX en el frontend: el filtro viejo ocultaba en silencio la propuesta recién creada, y además una partida recién importada nace en fase `'contabilidad'` mientras la pestaña por defecto de la vista es `'tesoreria'` — doble motivo para no verla. **Fix**: `cargaConfirmar()` (`View_Pagos.html`) ahora, tras un `confirmarCargaPropuestaPago` exitoso, ajusta `state.selectedSociedad` a la sociedad recién cargada si fue una sola (o lo limpia a `null` si se cargaron varias sociedades a la vez, para no dejar ninguna oculta), y fuerza `state.activeTab = 'contabilidad'` — el usuario aterriza directo en lo que acaba de crear. El camino de error no toca ninguno de los dos campos. `revisor` aprobó en el 1er intento (trazó el string de sociedad de punta a punta sin ninguna divergencia de formato). `qa` confirmó `LISTO` con 6 casos (filtro de otra sociedad se reemplaza, sin filtro previo también se ajusta, 2+ sociedades limpia el filtro, carga fallida no toca nada, salto automático a la pestaña correcta, el usuario puede seguir cambiando el filtro a mano después). Pusheado y verificado byte a byte contra el servidor. Desplegado a producción (`@27`). |
| TESO Tesorería 360 — Bloque D: correo al cargar una propuesta nueva | TERMINADO | Pedido original: "no veo que se envíen los correos" al cargar. La etiqueta `CARGADA` ya estaba reservada desde el Bloque A pero nunca se disparaba. **Implementado**: constante `TRANSICION_CARGADA` (`{tipo:'cargado', verbo:'cargada', etiqueta:'CARGADA'}`, mismo shape que devuelve `_tipoTransicionNotificable` para las 6 transiciones reales, pero literal porque la carga no es un patch sobre una partida existente). `_notificarTransicionPagos` se refactorizó para recibir la `transicion` ya resuelta en vez de un `patch` crudo (antes la computaba internamente vía `_tipoTransicionNotificable(patch)`, lo cual no aplicaba a la carga) — los 4 llamadores (`updatePartidaPago`, `bulkUpdatePartidasPago`, `aplicarPagoParcial`, `confirmarCargaPropuestaPago`) se actualizaron a la nueva firma. `confirmarCargaPropuestaPago` ahora captura los IDs de las partidas recién creadas (antes se generaban inline sin quedar accesibles) y dispara la notificación en el `finally`, DESPUÉS de liberar el `LockService` (mismo patrón ya usado en `aplicarPagoParcial` del Bloque B) — nunca si la carga fue rechazada por validación o UUID duplicado. `revisor` aprobó en el 1er intento. `qa` confirmó `LISTO` con 9 casos (1 sociedad dispara 1 correo con todas sus partidas, 2 sociedades disparan 2 correos separados, sociedad sin destinatarios no manda nada sin error, carga rechazada por validación/UUID no dispara ningún correo, bloque de auditoría vacío para partidas recién nacidas, las 3 transiciones existentes siguen funcionando igual tras el refactor de firma, patch no notificable sigue sin mandar correo, fallo de `MailApp` no propaga excepción). Pusheado y verificado byte a byte contra el servidor. Desplegado a producción (`@27`). |
| TESO Tesorería 360 — Re-prueba de integración FINAL (Bloques A+Auditoría+B+C+D juntos) antes del deploy final de la ronda 2026-07-27 | TERMINADO | Pedida explícitamente por el usuario ("termina todos los bloques pendientes y liberamos todo a producción"): recorrido de punta a punta del ciclo completo (cargar→revisar→autorizar parcial/total→liberar→subir→capturar cuenta→aplicar parcial→aplicar el resto→correo final), verificando que TODOS los bloques de la ronda funcionan bien JUNTOS. 8 casos de integración trazados: ciclo feliz completo con las 7 transiciones y sus etiquetas correctas, interacción Bloque D×Auditoría (notificación de carga tras liberar el lock no colisiona con el candado `estadoActual`), interacción Bloque B×D (refactor de `_notificarTransicionPagos` sigue funcionando en `aplicarPagoParcial`), interacción Bloque B×candado (partida con pago parcial rechazada sigue bloqueada), header/auditoría misma fuente de identidad, sin residuos de las funciones temporales, sin llamadores con la firma vieja de notificación, sin riesgo de pérdida de datos. **`LISTO PARA PRODUCCIÓN`.** 2 notas no bloqueantes para seguimiento futuro (no forman parte de esta ronda): (1) el cálculo de caducidad en `getPropuestasPago` no excluye partidas ya `pagado===true`, en teoría una partida pagada podría reclasificarse como "caducada" en la lectura si pasa mucho tiempo — lógica preexistente, no nueva de hoy, sin pérdida de datos real (es cálculo de lectura); (2) "Rechazar" solo tiene botón visible en la pestaña Tesorería — el backend ya permite rechazar en cualquier fase (protegido por el candado nuevo), pero no hay UI para hacerlo fuera de esa pestaña, consistente con la decisión explícita de dejarlo fuera de alcance tomada en el Bloque Auditoría. |
| TESO Tesorería 360 — Bloque E: CC en correos de pagos (estándar por sociedad + extraordinario por propuesta) + buscador de directorio de Google Workspace | TERMINADO | Pedido: además de "Notificaciones de Pagos" (destinatario principal/TO), una lista de copia (CC) basada en quién debe ver el proceso por sociedad, más la posibilidad de copiar a alguien extra solo para una propuesta puntual. **Implementado — Parte 1 (CC estándar)**: hoja `CONFIG_CC_PAGOS` (mismo shape que `CONFIG_NOTIFICACIONES_PAGOS`), nueva pestaña "CC de Pagos" **dentro** de la pantalla "Lista de Distribución" ya existente (decisión explícita del usuario, un solo archivo con pestañas internas — mismo patrón que `View_Pagos.html` — en vez de una pantalla aparte como se hizo con Notificaciones). Aplica a los 7 tipos de correo sin excepción. Gate Tesorería+Contador, igual que el resto del módulo. **Parte 2 (CC extraordinario)**: hoja `CC_EXTRA_PROPUESTA` (patrón de auditoría dedicada, como `APLICACIONES_PAGO`), capturado al cargar el Excel en `View_Pagos.html` con buscador de autocompletado contra el **directorio de Google Workspace** de la organización (no contactos personales — decisión explícita tras explicar la diferencia de permisos). Dura mientras la propuesta esté activa; sin lógica de expiración (una propuesta en fase final deja de generar correos). **Backend**: `_enviarCorreoNotificacionPago` combina CC estándar + CC extra + dedupe; `_notificarTransicionPagos` sub-agrupa cada sociedad por "firma de CC extra" para que el CC de una propuesta nunca se filtre hacia partidas de otra propuesta del mismo lote; `buscarContactosDirectorio` usa el servicio avanzado People (`People.People.searchDirectoryPeople`). Helper nuevo `_guardarListaPorSociedad` extraído de paso (DRY, reusado por Notificaciones y CC). `revisor` aprobó en el 1er intento. `qa` confirmó `LISTO` con 12 casos (sin CC configurado sale igual que antes, CC estándar solo, CC extra solo, ambos combinados con dedupe, lote de 2 propuestas sin cross-contaminación de CC, carga sin CC extra no escribe filas basura, búsqueda <3 caracteres no golpea la API, fallo de People API no rompe la pantalla, no duplica el mismo contacto, gate de rol consistente, tab "Reporte de Posición" intacta tras el rewrite, catálogo vacío sigue fallando abierto). **Incidente real durante el despliegue**: el primer intento de `appsscript.json` usó `serviceId:"people"` (no existe) — `clasp push --force` lo rechazó con "Service not found: people v1"; se pidió al usuario habilitar el servicio manualmente desde el editor de Apps Script (Servicios → + → People API), y al jalar el manifest real generado por esa acción se confirmó el `serviceId` correcto: `"peopleapi"` — corregido y verificado. Pusheado y verificado byte a byte contra el servidor (`clasp pull` + diff, incluye `appsscript.json`). Desplegado a producción (`@28`) — este despliegue pedirá reautorización a todos los usuarios la primera vez que usen la app (permiso nuevo de directorio), ya aceptado explícitamente por el usuario de antemano. |
| TESO Tesorería 360 — Bug real post-@28: crash `appendChild`/`Node` al expandir una propuesta recién cargada + propuesta "Ventura" invisible | TERMINADO (confirmado por el usuario 2026-08-02: la propuesta "Ventura" ya no presenta el problema) | Reportado por el usuario con stack trace real del navegador. Diagnóstico vía subagente `revisor` (búsqueda dirigida con el stack trace, no especulación): `l.numeroProveedor` a veces llega como JS `Number` (no `String`) porque Sheets auto-convierte texto puramente numérico a celda tipo Number al escribirse sin forzar formato de texto, y `_pagoSheetToObjects` (`Svc_Pagos.js`) no coacciona el valor de vuelta a `String` al leerlo — `VEVA.dom.el` truena al recibir un `Number` crudo como hijo del DOM (`appendChild` solo acepta `Node`/maneja `string` explícito). Explica el patrón exacto reportado: propuestas nuevas nacen colapsadas (`state.expandedProp`), así que el código de render defectuoso no se ejecuta hasta que el usuario expande manualmente esa propuesta — de ahí "carga sin error, crash al interactuar después en la misma sesión, sin crash en sesión nueva". **Fix implementado (pendiente de revisión/qa/deploy)**: `View_Pagos.html` — coerción `String(...)` en los 5 sitios que pasaban `numeroProveedor`/`referencia` crudos como hijo de `VEVA.dom.el` (`renderLineRow` línea ~679 y ~802, `renderProveedorGrupo` línea ~833, `renderCargaLineaRow` línea ~1171 y ~1173). `Svc_Pagos.js` — dentro de `confirmarCargaPropuestaPago`, se fuerza `setNumberFormat('@')` en las columnas `NUMERO_PROVEEDOR` (col 4) y `REFERENCIA` (col 7) de `PARTIDAS_PAGO` ANTES del `setValues`, para que un código puramente numérico ya no se autoconvierta a Number en la celda (arregla la causa raíz para cargas futuras; el `String()` en el frontend cubre además filas ya existentes con el tipo incorrecto). `revisor` aprobó en el 1er intento (confirmó índices de columna correctos, orden `setNumberFormat`→`setValues` correcto, escritura ya dentro del `LockService` existente sin ventana nueva, alcance completo — grep confirmó que `numeroProveedor` no se usa en ningún otro `View_*.html`; halló como nota no bloqueante que `l.desc`/`l.solicitante` tienen el mismo patrón estructuralmente inseguro pero con riesgo real bajo por ser texto libre, no códigos SAP numéricos — no ameritó rechazo). `qa` confirmó `LISTO` con 7 casos trazados (Number puro, vacío/null/undefined sin mostrar "undefined"/"NaN", String histórico sin regresión, preview de carga con datos de CSV siempre String, fila antigua ya corrompida en Sheets cubierta retroactivamente por el `String()` del frontend, `setNumberFormat` dentro del mismo `LockService` sin ventana nueva, ninguna otra función depende del tipo Number/String de estos campos). Pusheado y verificado byte a byte contra el servidor (`clasp pull` a directorio limpio + diff, `View_Pagos.html` y `Svc_Pagos.js` coinciden exacto). Desplegado a producción (`@29`), confirmado por el usuario. **Confirmado por el usuario 2026-08-02**: el fix también resolvió la visibilidad de "Ventura" — mismo origen, sin necesidad de una ronda de investigación aparte. |
| Proceso — nuevo subagente `analista` (comité de expertos) | TERMINADO | Pedido explícito del usuario, como PRIMERA prioridad antes de tocar cualquier otra cosa de la ronda 2026-07-28: un paso previo a `planificador` que garantice entendimiento mutuo real de cada solicitud — restablece la petición en sus propias palabras, separa hechos verificados (con cita archivo+línea) de supuestos, señala toda ambigüedad con sus interpretaciones posibles nombradas, y evalúa causa raíz vs síntoma antes de dejar planificar/codificar nada. Veredicto obligatorio `CONTEXTO_COMPLETO`/`FALTA_ACLARAR`. Creado `.claude/agents/analista.md` (mismas herramientas de solo lectura que `planificador`: Read/Grep/Glob, modelo sonnet). `CLAUDE.md` actualizado: nuevo paso 0 del flujo obligatorio (antes del `planificador` original, que pasa a ser el paso 1) — se usa cuando la solicitud es ambigua, grande, multi-parte, o trae dudas explícitas; se salta en tareas ya inequívocas y acotadas. No pasó por el ciclo `revisor`/`qa` (es configuración de proceso, no lógica de negocio de la app — mismo criterio ya usado para cambios de `PROJECT_STATUS.md` o de este mismo archivo). |
| TESO Tesorería 360 — Ronda de pedidos 2026-07-28 (7 piezas: header con nombre real, matriz de acceso admin, duda de duplicación Lista de Distribución/CC, 2 bugs reales de workflow + rediseño checkbox por columna, selector de columnas tipo SAP con plantilla en BD_TESO, bug visual de letras sobrepuestas, endurecer notificación seccionada + candado de fase) | EN_PROGRESO (parcial — actualizado 2026-08-02) | **Resuelto desde entonces, en filas propias**: los 2 bugs reales (TC inalcanzable en moneda extranjera, `bulkUpdatePartidasPago` sin reportar fallas parciales) y el rediseño de checkbox por columna en Banca/Aplicado + notificación seccionada → ver fila "Bloque F" (`TERMINADO`). El "bug visual de letras sobrepuestas" descrito abajo coincide con el bug de encabezados amontonados/superpuestos arreglado esta sesión (2 filas `TERMINADO` más abajo). **Genuinamente sigue abierto**: header con nombre real (People API, solo falta cambiar el email mostrado por el nombre), matriz de acceso admin de solo lectura, fusión de "Notificaciones de Pagos"+"CC de Pagos" en una sola pantalla, selector de columnas tipo SAP con plantilla en BD_TESO, y confirmación del usuario sobre la hipótesis del "candado de fase". Decisiones ya tomadas con el usuario vía preguntas directas: (1) matriz de acceso admin = reporte de SOLO LECTURA, no fuente de control real; (2) el patrón botón+input de la pestaña Aplicado se reemplaza por un checkbox de estado ("Aplicada"/"Rechazada") por columna; (3) la plantilla de columnas para el selector tipo SAP se diseña desde cero con base en la estructura real de hoy de "Propuesta" (no existe todavía como hoja separada en BD_TESO), y debe sevir TAMBIÉN como validación de la carga de Excel (rechazar si el archivo subido no coincide con la plantilla). 2 bugs reales ya confirmados leyendo código (evidencia, no reporte sin verificar): **Bug A** (`View_Pagos.html` líneas 733-742) el campo TC en la pestaña Aplicado solo se habilita si se convierte la moneda de pago a MXN — si la partida se paga en su propia moneda extranjera (USD/GBP/EUR) el TC queda inalcanzable, aunque el usuario sí necesita registrarlo. **Bug B** (`Svc_Pagos.js` `bulkUpdatePartidasPago` línea 1053) si una acción masiva (Liberar/Marcar subidas/Aplicar pagos) tiene alguna partida que falla validación, las que sí pasaron ya quedaron escritas en la hoja pero la respuesta completa vuelve `status:'error'` sin decir cuáles pasaron/cuáles no ni guardar el motivo en ningún lado — probablemente la causa de varias rarezas reportadas del workflow. **Decisiones ya cerradas con el usuario (ronda de preguntas 2)**: (4) SÍ se fusionan "Notificaciones de Pagos" (TO) y "CC de Pagos" en una sola pantalla; (5) el patrón de checkbox-por-columna NO se extiende a todas las fases — se queda tal cual en el resto (Liberar a Tesorería/Autorizar/Liberar seleccionadas sin cambio) y aplica ÚNICAMENTE a las 2 fases operadas por Tesorería: "Banca" (subir) y "Aplicado" (aplicar pago). Especificación exacta dada por el usuario para esas 2 fases: 2 columnas de checkbox por línea — "Aplicar/Subido" y "Rechazar/No pagado" (el nombre exacto de la 2ª cambia según la fase). El botón de acción del lote libera SOLO las líneas marcadas en la columna positiva; las marcadas como rechazado/no pagado NO avanzan. Si una línea no tiene NINGUNA de las 2 columnas marcada, se queda en standby en su fase actual — no avanza y tampoco genera notificación. El correo de esa transición sale seccionado en 2 partes: liberadas/aplicadas vs. no liberadas/no aplicadas (con su motivo). (6) la plantilla tipo SAP es UNA sola hoja que sirve para vista Y validación de carga de Excel a la vez (un renglón por campo: nombre en Excel, campo interno, en qué fases se muestra), diseñada desde cero tomando como base `PROPUESTA_HEADERS_REQUERIDOS` (`Svc_Pagos.js:1304`, hoy hardcodeado) y la estructura real de "Propuesta". **Pendiente aún**: el usuario no pudo confirmar ni descartar mi hipótesis sobre el "candado de fase" (relacionado con el Bug B) con la explicación técnica que le di — pedí una reexplicación en términos simples y, si vuelve a pasar, capture de pantalla + pasos exactos; captura de pantalla pendiente del bug visual de letras sobrepuestas (no se puede diagnosticar sin verlo). Header con nombre real de Google (ya muestra rol, solo falta cambiar el email por el nombre vía People API self-lookup), matriz de acceso de solo lectura, y fusión de listas de correo son de bajo riesgo/ambigüedad — listos para pasar directo a `planificador`. Siguiente paso: despachar `planificador` para el bloque de mayor prioridad (bugs reales + rediseño de checkbox en Banca/Aplicado + notificación seccionada), ya con alcance exacto confirmado por el usuario. |
| TESO Tesorería 360 — Bloque F: checkbox doble (Aplicar/Rechazar) en Banca+Aplicado + notificación seccionada + fix TC moneda extranjera + fix `bulkUpdatePartidasPago` | TERMINADO | `planificador` entregó plan completo. 3 decisiones abiertas resueltas por el usuario: (1) SÍ se mantiene el input de monto parcial junto al checkbox positivo en Aplicado (no se pierde el Bloque B de ayer); (2) el correo reusa la etiqueta genérica "RECHAZADA" ya existente; (3) las 2 columnas se auto-excluyen entre sí. **Implementado**: `View_Pagos.html` — Banca/Aplicado rediseñadas con 2 checkboxes efímeros por línea (`l._resolverPositivo`/`_resolverNegativo`, nunca persistidos, se resetean solos en cada `cargar()`), grid de 9 columnas solo en esas 2 pestañas (`colHeadStyle`/`COL_HEAD_STYLE_RESOLVER`), botón de lote nuevo `resolverBanca`/`resolverAplicado` (reemplazó `toggleBool`/`aplicarPagosSeleccionados`/`aplicarMontoParcial`/`renderStatusAplicado`, todos retirados); fix del TC (línea ~741, ya no depende de convertir a MXN, siempre capturable si `moneda!=='MXN'`, con "≈ MXN $X" visible). `Svc_Pagos.js` — `_aplicarPagoParcialCore` extraído del cuerpo de `aplicarPagoParcial` (mismas 5 validaciones intactas: estado/fase/cuenta/monto/tolerancia $0.01) para reusarlo en `bulkResolverAplicado` con lock por partida (mismo criterio que el individual, escribe en `APLICACIONES_PAGO`); `bulkResolverBanca` sin lock (mismo patrón ya existente en `bulkUpdatePartidasPago` para mutaciones que no tocan ese libro); `_notificarTransicionPagosMultiple`/`_enviarCorreoNotificacionPagoSeccionado` nuevos (agrupan por sociedad + firma de CC extra + transición, arman 1 correo con N secciones) — cuando hay 1 sola sección delegan a `_enviarCorreoNotificacionPagoUnica` (el HTML original, sin una sola línea tocada, para no arriesgar el template ya probado en producción); `bulkUpdatePartidasPago` cambia su contrato a `status:'success'|'parcial'|'error'` con `data.idsExitosos`/`data.errores`. Cero cambios a `contabilidad`/`tesorería`(autorizar)/`liberación2` — verificado pixel-idénticos. Cero migración de esquema. `revisor` rechazó 1 vez (código muerto: quedó un wrapper `_enviarCorreoNotificacionPago` sin sufijo que nadie llamaba, contradecía su propio comentario de cabecera) — corregido (borrado), aprobado en el 2° ciclo. `qa` confirmó `LISTO` con 8 casos trazados (lote mixto en Banca sin tocar la línea sin marcar, monto parcial en Aplicado no dispara notificación hasta completar 100%, fallback a saldo completo sin monto capturado, bloqueo frontend sin cuenta de pago, TC informativo sin alterar el monto exigido, `bulkUpdatePartidasPago` con éxito parcial ya no es `'error'` opaco, lote 100% fallido no notifica, transición de 1 sola línea sigue usando el HTML original sin cambios). Pusheado y verificado byte a byte contra el servidor. Desplegado a producción (`@30`), confirmado por el usuario. |
| TESO Tesorería 360 — Auditoría profunda del workflow completo (usuario/programador/auditor), pedida antes de desplegar Bloque F | TERMINADO | Subagente `revisor` en modo auditor (modelo Opus) encontró 3 hallazgos críticos financieros (C-1/C-2/C-3), 5 bugs funcionales (B-4 a B-8) y 8 de deuda técnica/cosmético (D-9 a D-16, M-17 a M-20) sobre TODO el módulo, no solo el diff de Bloque F. **Resueltos directamente (bajo riesgo, sin ambigüedad)**: B-4 (pestaña Conciliación usaba `VEVA.fmt.mxn`, redondeaba a pesos enteros con tolerancia de negocio <$1 y etiquetaba MXN montos que en realidad estaban en otra moneda — reemplazado por `fmtMonto`/`fmtMontoDivisa` en los 6 sitios reales, `View_Pagos.html`); B-8 (`getTcActual`/`getCatalogoCuentasPago` fallando en `cargar()` se descartaban en silencio — ahora se acumulan como aviso en `state.error`, no bloqueante). **C-2 (re-escalado del objetivo al editar TC/moneda/monto autorizado tras pagos parciales) — decisión explícita del usuario tras 3 rondas de aclaración con ejemplos concretos: NO SE IMPLEMENTA.** El usuario confirmó que este sistema no busca ser un libro contable estricto tipo SAP que cuadre centavo a centavo — el control real ya lo hace el contador al revisar la propuesta al subirla, y el congelamiento propuesto era más rigidez de la que el negocio necesita. Se ofreció también un fix mínimo de 1 línea (candado bidireccional que detecte "se pagó de más", sin ningún congelamiento de campos) — el usuario también lo rechazó explícitamente ("no, déjalo como está"). **Pendiente de implementar**: C-1 (`_validarTransicionPermitida` default-allow — un patch `{fase:'aplicado'}` o `{estado:'activa'}` sueltos, sin su firma completa, pasan sin ningún control) y D-9 parcial (rama `idsNoPagado` de `bulkResolverAplicado`, código de HOY en Bloque F, sin el mismo `LockService` que ya protege a su rama hermana `aplicar`) — `planificador` ya entregó el plan para ambos, verificado contra las 7 transiciones legítimas reales del workflow, sin objeción del usuario. **Hallazgo nuevo de la ronda de aclaración** (no estaba en la auditoría original): el mismo hueco que motivó C-2 también existe en `montoAutorizado` (el techo que Tesorería fija al autorizar) — documentado aquí como deuda técnica conocida y aceptada, no se corrige, mismo criterio que el usuario aplicó a C-2. **Diferido a una ronda aparte** (confirmado por el usuario): mejorar el control de UUID duplicado al cargar Excel — hoy `confirmarCargaPropuestaPago` rechaza TODA la carga si algún UUID ya existe en `PARTIDAS_PAGO`, sin importar el estado de esa partida existente ni dar opción de confirmar una resubida intencional. **Aceptado como deuda técnica sin acción** (triage del propio auditor, sin objeción): C-3 (caducidad no exime partidas ya en fase banca/aplicado), D-10 a D-16 (silenciamientos menores, duplicación de HTML de correo, lecturas repetidas de hoja en `bulkResolverAplicado`, sin función de reversión de pago aplicado, partidas huérfanas invisibles, patrón optimista sin confirmar en `bulkAction` de las 3 fases que Bloque F no tocó, 3 sitios residuales del patrón `Number` sin `String()` en proveedor/desc de Conciliación/Comprobantes), M-17 a M-20 (código muerto menor, HTML del correo sin escapar, sin confirmación en acciones de rechazo masivo). **C-1 y D-9 implementados**: `_validarTransicionPermitida` (`Svc_Pagos.js`) gana 2 bloques nuevos — (a) cualquier patch con `fase` distinta de `'tesoreria'` se rechaza salvo que venga acompañada exactamente de la transición real que autoriza ese destino (`liberacion2`+`status:'autorizada'`, `banca`+`status:'liberada'`, `aplicado`+`subido:true`); (b) `estado:'activa'` sin la firma completa de reactivación (`reactivacionSolicitada:false`) se rechaza explícito en vez de caer al `{ok:true}` por defecto. `bulkResolverAplicado`: la rama `idsNoPagado` ahora toma el mismo `LockService` por id que ya usaba su rama hermana `aplicar`. `revisor` aprobó en el 1er intento (rastreó las 7 transiciones legítimas reales contra sus llamadores en código, confirmó que los 2 patches maliciosos originales quedan rechazados, confirmó que el lock nuevo no rompe la recolección de `errores`/`idsRechazadosExitosos`, sintaxis verificada con `node -c`). `qa` confirmó `LISTO` con 7 casos trazados (ataque de fase suelta, ataque de reactivación suelta, ataque mezclado status+fase ajena, las 7 transiciones legítimas sin falso rechazo, patch trivial sin gate como antes, error aislado en lote de `idsNoPagado` sin dejar el lock tomado, conteos finales sin corrupción). Pusheado y verificado byte a byte contra el servidor. Desplegado a producción (`@30`), confirmado por el usuario. |
| TESO Tesorería 360 — Totales por fase (moneda origen + MXN) + override de UUID duplicado con motivo obligatorio | TERMINADO | Pedido explícito del usuario: (1) en las 5 fases del workflow, la barra de resumen de la acción masiva debe mostrar cuánto se mueve, desglosado por moneda de origen más su equivalente en MXN (cierra de paso un hallazgo de la auditoría anterior: antes mezclaba monedas distintas en una sola cifra sin sentido, o en Banca/Aplicado solo mostraba conteos sin monto); (2) la carga de Excel ya no debe rechazar en automático un UUID ya existente — debe pedirle a la misma persona que carga que confirme explícitamente con un motivo obligatorio, dejando rastro auditable. **Pieza 1 implementada**: helpers nuevos `resumenChipsTexto(lineas, montoGetter)` (desglose por moneda + "≈ MXN $Z (TC W)", mismo patrón que ya usaba la barra sticky) y `_saldoPendienteAplicar(l)`/`montoResolverAplicado(l)` (fuente única del saldo pendiente/monto a aplicar, reusada tanto por el resumen como por la acción real de `resolverAplicado`, para que nunca diverjan). Contabilidad/Tesorería/Liberación2 muestran 1 línea de resumen; Banca/Aplicado muestran 2 (positivo en negrita = lo que se libera/aplica, negativo atenuado = lo que se rechaza/no paga, solo si hay alguno). Los checkboxes `cb`/`selAllCb` en Banca/Aplicado (sin efecto real desde el Bloque F) quedan deshabilitados con tooltip explicativo, sin tocar las otras 3 fases. **Pieza 2 implementada**: `confirmarCargaPropuestaPago` (`Svc_Pagos.js`) cambia de recibir el arreglo de propuestas pelado a recibir `{propuestas, forzarDuplicados, motivoForzado}`; si hay UUID duplicados sin forzar, rechaza igual que siempre; si se fuerza sin motivo, rechaza en el SERVIDOR (nunca confía solo en el cliente); si se fuerza con motivo, continúa la carga y registra una fila por línea forzada en la hoja nueva `OVERRIDE_DUPLICADOS_PAGO` (quién, cuándo, UUID, proveedor, sociedad, motivo), dentro del mismo `LockService`/bloque de escritura que ya usan `PROPUESTAS_PAGO`/`PARTIDAS_PAGO`/`CC_EXTRA_PROPUESTA`. Excluir la línea duplicada con el checkbox que ya existía sigue funcionando igual, sin pasar por el flujo de override. El frontend agrega un textarea de motivo obligatorio en el propio panel de vista previa (visible solo si quedan duplicados sin excluir), bloqueando el botón "Confirmar carga" hasta que se capture. `revisor` rechazó 1 vez (fórmula divergente: `montoResolverAplicado` tenía un fallback de saldo distinto al de `renderCeldasAplicado`, enmascarado porque el backend siempre manda `saldoPendienteAplicar` ya calculado; y `confirmarCargaPropuestaPago` excedía la convención de funciones cortas del proyecto) — corregido extrayendo `_saldoPendienteAplicar(l)` como fuente única y `_filaOverrideDuplicado(...)` como helper de la fila de auditoría, aprobado en el 2° ciclo. `qa` confirmó `LISTO` con 10 casos trazados (desglose multi-moneda sin sumar sin sentido, Banca con positivo/negativo sin cruzarse, monto parcial capturado coincide entre resumen y acción real, listas vacías sin romper, checkboxes de las otras 3 fases intactos, rechazo sin forzar, rechazo server-side con motivo vacío, carga+auditoría con motivo real, línea excluida nunca pide motivo ni se audita, 2 líneas forzadas en 2 sociedades sin cruzar PROPUESTA_ID/PARTIDA_ID). Pusheado y verificado byte a byte contra el servidor. Desplegado a producción (`@30`), confirmado por el usuario, junto con Bloque F y los fixes de auditoría C-1/D-9. |
| TESO Tesorería 360 — 20 pruebas intrincadas de regresión sobre el workflow completo, pedidas por el usuario tras el deploy `@30` | TERMINADO | 2 tandas de 10 casos vía `qa`, combinando varias piezas a la vez (no pruebas triviales de 1 sola funcionalidad): ciclo feliz de 6 transiciones, autorización parcial + aplicación en 2 exhibiciones, lote multi-moneda en Banca (MXN/USD/GBP/SIN_MONEDA) con positivas y negativas mezcladas, 2 ataques directos al backend saltándose el frontend (fase suelta y reactivación suelta — ambos ya cerrados por C-1), lote mixto en `bulkResolverAplicado` con éxito parcial, override de UUID duplicado en 2 sociedades sin cruzarse, rechazo del lote sin forzar, exclusión manual + forzado combinados en el mismo Excel, GBP excluido a propósito del blended MXN, monto parcial editado a mano en un lote mixto de Aplicado, concurrencia simulada sobre la misma partida (candado correcto), CC extra + CC estándar combinados en correo seccionado sin cruzarse entre propuestas, caducidad de partida temprana, bloqueo total del lote sin cuenta de pago (defensa en 2 capas), multi-rol ejecutando pasos de 2 roles el mismo día, auditoría de correo tras rechazo+reactivación (comportamiento confirmado, no es bug: `RECHAZADO_POR`/`REACTIVADO_POR` no están en `_AUDITORIA_FASES_CORREO` a propósito), consistencia entre barra sticky y barra por fase, checkboxes deshabilitados sin contaminar `l.checked` entre pestañas. **18 de 20 casos: LISTO.** **1 hallazgo real (casos 16+19, misma causa raíz)**: los chips de total a nivel PROPUESTA y SOCIEDAD (`chipsProp`/`chipsSoc` en `View_Pagos.html`, dentro de `renderPropuestaNode`/`renderSociedadNode`) sumaban `l.monto` crudo vía `totalesPorDivisa`, sin respetar `montoEfectivo` en líneas con autorización parcial (`approvalType==='parcial'`) — sobreestimaban el total, y divergían del total correcto que ya mostraba la barra sticky (`renderResumenSeleccion`, que sí usa `montoEfectivo`). Bug preexistente, no introducido por el Bloque F ni por la ronda de totales anterior — destapado por esta ronda de pruebas. **Corregido**: ambos sitios ahora envuelven las líneas con `montoEfectivo(l)` antes de pasarlas a `totalesPorDivisa`, mismo patrón que ya usa `resumenChipsTexto`; se preservó el alcance original (`chipsProp`/`chipsSoc` siguen incluyendo TODAS las líneas, también rechazadas/caducadas, a propósito — solo se corrigió la fórmula del monto, no el filtro de líneas). `revisor` aprobó en el 1er intento (confirmó `montoEfectivo` correcto en ambos sitios, alcance preservado sin filtro de estado, `granTotalConTc` sigue funcionando sobre el nuevo shape, sin otro sitio residual con el mismo bug). `qa` confirmó `LISTO` con 4 casos trazados (línea total+parcial suma $13,000 no $20,000, chip de propuesta coincide ahora con la barra sticky para la misma selección, línea rechazada sigue incluida en el total de la propuesta, "≈ MXN" se calcula bien sobre los chips ya corregidos). Pusheado y verificado byte a byte contra el servidor. Desplegado a producción (`@31`), confirmado por el usuario. |
| TESO Tesorería 360 — Segundo enlace de producción descubierto y sincronizado | TERMINADO | El usuario compartió una URL de producción distinta a la usada hasta ahora en la sesión (`AKfycbzpHYuzbGNxYK3_bv3OgUZh5QnLugJcpp9pIht2GbGN7wcHFM4XIE-XqKgwrqCAVv7FfQ`). Investigado con `clasp deployments`/`clasp versions` antes de asumir nada: mismo proyecto de Apps Script, pero un ID de despliegue distinto al de siempre (`AKfycbwYxMVPHDWthysYPhnZ_jLccJXxw3VcLDK6iLf-sLfuMX4HGMoKC9xzx6C5ymqL7IOX_A`, en `@31`), apuntando a la versión `@32` ("`.63`") creada justo después de mi último deploy, sin ningún `clasp push` de por medio en mis acciones. El usuario confirmó que él mismo creó ese segundo despliegue manualmente desde el editor de Apps Script. Sincronizado a petición explícita del usuario: `clasp deploy -i` corrido también sobre ese segundo ID, ahora en `@33` con el mismo código exacto que `@31`. Memoria de referencia (`reference_teso_deployment.md`) actualizada para reflejar que de aquí en adelante hay que desplegar a AMBOS IDs, no solo al original — de lo contrario uno de los dos enlaces se queda desactualizado en silencio. |
| TESO Tesorería 360 — Validación de cargas de saldos del 2026-07-30 (12 archivos) + fix de raíz en el parser para evitar recurrencia | TERMINADO | Pedido explícito del usuario: validar uno a uno, contra los archivos fuente reales (carpeta local `cargasaldos`, sincronizada con Drive), que la carga de saldos bancarios del día quedara correcta en `HIST_POSICION_BANCARIA`. **Diagnóstico** (`diagnosticoSaldosHoy`, función temporal de solo lectura): 56 cuentas cruzadas contra 12 archivos fuente (6 CSV Santander, 2 CSV Banorte movimientos, 2 CSV Banorte resumen, 1 XLSX Inbursa, 1 XLS legado BBVA) — 1 solo discrepancia real: cuenta Banorte 406605300 quedó con SALDO $46,706.80 en vez del cierre real $29,635.88. **Causa raíz confirmada leyendo código real** (`Svc_Parser.js`, `procesarArchivoSaldos`): un archivo de MOVIMIENTOS de todo julio (150+ filas de esa cuenta, con una columna literalmente llamada "SALDO") pasó por error el flujo de carga de SALDOS; el parser no validaba 1-fila-por-cuenta y asignaba la misma fecha a cada fila, y `guardarSaldosEnHistorial` deduplica por `fecha+cuenta` quedándose con la PRIMERA fila del archivo — así sobrevivió la transacción del 01/07 en vez del cierre real. **Corrección puntual** (`fixSaldoBanorte406605300`, función temporal): actualiza la fila existente (sin duplicar, pedido explícito del usuario), con guardas verificadas por `revisor` en 2 rondas (confirma MONEDA=MXN/TC=1 y el SALDO erróneo conocido antes de escribir; aborta sin tocar nada si no calzan). Bug real detectado en el primer intento del usuario: la función buscaba fecha=HOY (`new Date()`) pero un día ya había pasado desde la carga original (30/07→31/07) — corregido a fecha fija `'2026-07-30'`. **Solución de raíz** (pedido explícito: "que sea de fondo la solucion de raiz en el parser"): nuevo helper `_validarCuentasUnicas` en `Svc_Parser.js`, llamado desde `procesarArchivoSaldos` — si cualquier cuenta aparece más de 1 vez en el archivo, rechaza el archivo COMPLETO (`success:false`, todo-o-nada, sin guardar nada) con mensaje explícito sugiriendo que es un archivo de movimientos; aplica a los 5 bancos configurados, sin cambios en `Codigo.js` ni en el frontend (ambos ya propagaban `success:false` sin escribir). Diseño (error duro vs. advertencia, todo-o-nada, clave=solo cuenta sin moneda) pasado por `planificador` y aprobado explícitamente por el usuario antes de codificar. `revisor` rechazó 1 vez (función debía extraerse a helper dedicado + inconsistencia de guion vs. em dash) — corregido, aprobado en el 2° ciclo. `qa` confirmó `LISTO` con 5 casos del helper puro (sin duplicados, 1 cuenta repetida 150 veces con mensaje correcto, array vacío, 2 cuentas duplicadas distintas listadas ambas, archivo de 1 sola fila) más verificación de que ambos callers (`previsualizarSaldos`/`guardarSaldosEnHistorial`) abortan sin escribir. Pusheado y verificado byte a byte contra el servidor (`Codigo.js` y `Svc_Parser.js`). Desplegado a producción en ambos IDs (`@34`/`@35`), confirmado por el usuario. **Pendiente**: que el usuario corra `fixSaldoBanorte406605300` (ya con la fecha corregida) para aplicar la corrección real en `HIST_POSICION_BANCARIA`; retirar ambas funciones temporales (`diagnosticoSaldosHoy`, `fixSaldoBanorte406605300`) una vez confirmado. |
| TESO Tesorería 360 — Ronda de retroalimentación de diseño 2026-07-31 (16 puntos, módulo Pagos) | TERMINADO | Usuario pidió pasar por `analista` antes de planificar (petición larga y desordenada, 16 puntos mezclados). `analista` restableció cada punto, verificó contra código real (varios ya resueltos por rondas anteriores, otros bugs reales confirmados), y devolvió `FALTA_ACLARAR` con 7 preguntas priorizadas — resueltas con el usuario antes de seguir. 2 puntos (#2 rechazo en Banca sin notificación separada, #16 "autorización partida por partida") se investigaron con una función de diagnóstico temporal de solo lectura contra datos reales de `BD_TESO` (`diagnosticoFeedbackPagos20260731`, 3 iteraciones — la primera tenía un bug propio: filtraba por `STATUS==='autorizada'` actual en vez de por presencia de `AUTORIZADO_FECHA`, excluyendo partidas ya avanzadas de fase) — sin evidencia de que sigan pasando hoy, quedaron en observación. `planificador` organizó los puntos confirmados en 5 grupos. **Grupo 1** (bajo riesgo): botón Cancelar faltante en la rama de error de carga de propuesta sin preview generado; soporte de EUR en la carga (opcional, no agregado a `PROPUESTA_HEADERS_REQUERIDOS` para no romper archivos existentes sin esa columna). **Grupo 2** (mayor riesgo, causa raíz): nueva función `recargarPropuestas()` que nunca toca `state.loading`, eliminando el colapso de la tabla a un spinner que causaba el salto de scroll al inicio de la página en cada mutación — reemplazó ~15 llamadas a `cargar()` usadas solo como recuperación de error; nuevo flag `state.accionEnProceso` + helper `renderBulkBtn` para feedback visual en los 5 botones de acción en lote. 2 rondas de rechazo por hallazgos reales de `revisor` (enmascaramiento de `state.error` por `recargarPropuestas()`, y un tercer patrón de llamado en `solicitarReactivacion` que se le escapó a la primera corrección). **Grupo 3**: el correo de notificación usaba `Number(l.monto)` crudo en vez de `_montoEfectivoPartida` (ignoraba autorización parcial); se agregó subtotal por moneda y columnas Referencia/UUID/fase-específica (solo la fase de esa transición, no las 5 acumuladas, decisión explícita del usuario); `qa` encontró celdas sin escapar HTML (`_escapeHtmlCorreo` nuevo, corregido). **Grupo 4**: controles "seleccionar todas"/"por proveedor" para Banca/Aplicado sobre los flags efímeros `_resolverPositivo`/`_resolverNegativo` (el `selAllCb` existente no aplica ahí desde el Bloque F) — `revisor` rechazó 1 vez por falta de gate de rol Tesorería en los botones de grupo-proveedor, corregido extrayendo `renderBotonesResolverGrupo`. **Grupo 5** (feature nueva): reporte histórico de pagos, diseño mockup-first iterado varias rondas con el usuario (Artifact), pantalla nueva `View_HistoricoPagos.html` + `getHistoricoPagos` (`Svc_Pagos.js`) — fuente única: `APLICACIONES_PAGO` (una fila por cada exhibición real de pago, confirmado que `_aplicarPagoParcialCore` escribe ahí siempre, no solo en parciales). Diseño de TC aclarado con el usuario: TC del día (referencia, hoy solo existe para USD) vs TC aplicado (el real de compra, capturado por Tesorería) — sin construir infraestructura nueva. `revisor` rechazó 1 vez por 2 hallazgos financieros reales: `toISOString()` agrupaba pagos por día en UTC en vez de hora México (bug de zona horaria real, un pago después de las 18:00 caía en el día siguiente), y `_getTcForDate` devuelve `1` (no `null`) cuando no hay dato — se hubiera mostrado como tasa real; se creó `_tcDelDiaOrNull` (sin tocar la función compartida `_getTcForDate`, usada por saldos/movimientos con su contrato actual) y se corrigió la agrupación con `Utilities.formatDate`. Nueva vista registrada en el menú (`Shared_Core.html`, sección TESORERIA) e incluida en `Index_1.html`. `qa` confirmó `LISTO` en los 5 grupos. Pusheado y verificado byte a byte contra el servidor en cada ronda. Desplegado a producción en ambos IDs (`@36`/`@37`). Las 3 funciones temporales de diagnóstico usadas durante la investigación (`diagnosticoSaldosHoy`, `fixSaldoBanorte406605300` de la ronda de saldos anterior, y `diagnosticoFeedbackPagos20260731` de esta ronda) se retiraron una vez confirmado que ya cumplieron su propósito — desplegado (`@38`/`@39`). |
| TESO Tesorería 360 — Bug real: carga de propuesta rechazaba "Soccer Loco Holding" por falta de sufijo numérico en "Nombre empresa" | TERMINADO | Usuario reportó que no podía subir una propuesta real (archivo `Propuesta de Pago 29 de Julio de 2026 SSL_Fibra Uno (1).xlsx`), 4 filas con "Sociedad no reconocida en CAT_CUENTAS_MAPEO: 'Soccer Loco Holding'". Diagnosticado leyendo el archivo real (no se asumió nada): la columna "Nombre empresa" trae literalmente "Soccer Loco Holding" **sin ningún número al final** — a diferencia de todas las demás sociedades ya vistas, cuyo "Nombre empresa" siempre trae un código pegado (ej. "Blau Life SAPI de CV 8332"), que es lo que `_resolverSociedadDesdeNombreEmpresa` usaba para buscar contra `ULTIMOS_DIGITOS`. Confirmado con un diagnóstico temporal de solo lectura contra `CAT_CUENTAS_MAPEO` real que la sociedad SÍ está dada de alta (3 filas de cuenta — MXN/USD/nómina — con `NOMBRE_SOCIEDAD='SOCCER LOCO HOLDING'`, mismo `NOMBRE_CORTO='SOCCER'` en las 3). Pedido explícito del usuario: solución general para cualquier propuesta (no un caso especial de Soccer Loco), sin tocar `CAT_CUENTAS_MAPEO`. **Implementado**: `_resolverSociedadDesdeNombreEmpresa` ahora tiene un fallback — si "Nombre empresa" no trae número al final, compara el texto completo (mayúsculas + trim) contra `NOMBRE_SOCIEDAD`; si SÍ trae número pero no matchea ningún `ULTIMOS_DIGITOS`, NO cae al fallback (sigue "no reconocida" como antes — un número presente-pero-equivocado es señal real de error, no debe enmascararse). `_leerCatCuentasPorUltimosDigitos` ahora también expone `nombreSociedad` y relajó su filtro (antes exigía `ULTIMOS_DIGITOS` no vacío, ahora solo `NOMBRE_CORTO`) sin afectar la búsqueda por código existente. `revisor` aprobó en el 1er intento (con una nota cosmética de wording en el comentario, corregida). `qa` confirmó `LISTO` con 7 casos (número normal, sin número con fallback, número equivocado sin caer al fallback, sin número y sin match en catálogo, cadena vacía, espacios extra antes del número, case-insensitive minúsculas). Pusheado y verificado byte a byte. Desplegado a producción en ambos IDs (`@40`/`@41`). Función de diagnóstico temporal (`diagnosticoCatCuentasSoccer`) retirada tras usarse. |
| TESO Tesorería 360 — Bug real: encabezados de columna amontonados/superpuestos en Pagos | TERMINADO | Usuario reportó encabezados ilegibles en todas las pestañas de Pagos, persistente incluso en ventana de incógnito (se descartó caché). Diagnosticado con el HTML real inspeccionado por el usuario en el navegador: `renderColHead`/`renderComprobantesHead` pasaban varias etiquetas como strings sueltos SEGUIDOS (ej. `['', 'Proveedor', 'Referencia', 'Descripción', ...]`) como hijos directos de un contenedor `grid` — comportamiento real de CSS (no bug de `VEVA.dom.el`): strings sueltos consecutivos se fusionan en una sola caja anónima compartida en vez de ocupar una celda cada uno, por eso "Proveedor"+"Referencia"+"Descripción" se veían amontonados en la primera columna (22px) mientras "Monto" (el único ya envuelto en su propio `<span>`) se veía bien. Bug preexistente desde antes de esta sesión, no una regresión nueva. **Fix**: nuevo helper `celdaHead(texto, claseExtra)` que envuelve cada etiqueta en su propio `<span>`, aplicado en las 2 funciones afectadas (`renderColHead`, `renderComprobantesHead`). Confirmado por grep en todo el proyecto que ningún otro lugar tenía el mismo patrón de riesgo. `revisor` aprobó en el 1er intento. `qa` confirmó `LISTO` (conteo exacto de columnas/hijos en los 3 casos de grid distintos, clase `text-right` de "Monto" preservada). Pusheado y verificado byte a byte. Desplegado a producción en ambos IDs (`@42`/`@43`). |
| TESO Tesorería 360 — Bug real: Histórico de Pagos ignoraba el TC real capturado cuando no hubo conversión formal de moneda | TERMINADO | Usuario reportó 2 partidas USD ($6,140.94 y $30,379.92), TC real capturado de 17.4312 en la pestaña "Aplicado", pero el reporte mostraba "Monto MXN" de $302.54 — sin relación con esas cifras. Causa raíz: `tcAplicadoNum` en `getHistoricoPagos` solo leía `TC_APLICADO` cuando `hayConversion` era true (`monedaPagoReal !== moneda`) — pero ese campo se puede capturar como dato informativo aunque la partida se pague desde una cuenta en su propia moneda extranjera, sin conversión formal (comportamiento ya establecido en `View_Pagos.html`, "el TC es solo informativo"). En ese caso real, el código descartaba el TC real y caía a `_tcDelDiaOrNull` (referencia genérica del día), produciendo un monto sin sentido. **Fix**: `tcAplicadoNum` ahora se calcula independientemente de `hayConversion`; en la rama sin conversión formal, "Monto MXN" da prioridad al TC real capturado sobre la referencia genérica (que solo se muestra aparte para comparación, cuando existe). `revisor` aprobó en el 1er intento (con nota menor de docstring desactualizado, corregida de inmediato). `qa` confirmó `LISTO` con 7 casos (caso real reportado, conversión formal, sin TC con fallback a referencia, sin TC y sin referencia, moneda no soportada pero con TC capturado, valores de TC inválidos, MXN nativa). Pusheado y verificado byte a byte. Desplegado a producción en ambos IDs (`@44`/`@45`). |
| TESO Tesorería 360 — Encabezados de columna seguían desalineados tras el fix anterior (raíz: CSS estático sin recompilar) | TERMINADO | El fix anterior (envolver cada etiqueta en su propio `<span>` vía `celdaHead`) resolvió que cada columna ocupara su propia celda de grid, pero el usuario mandó nueva captura mostrando texto aún desbordándose entre columnas angostas (ej. "Referencia" en 90px). Causa: sin `overflow:hidden`/`min-width:0`, una celda de grid no se recorta — crece más allá del ancho definido si el contenido no cabe. Agregado truncamiento (`truncate whitespace-nowrap min-w-0` + `title` con el texto completo al pasar el mouse) y reducido el tamaño de fuente del encabezado a pedido del usuario (`text-[9.5px]` → `text-[8.5px]`). **Segundo hallazgo real, más importante**: `revisor` rechazó la primera versión porque `text-[8.5px]`/`text-[9.5px]` NO estaban compilados en `Styles.html` (CSS estático sin Tailwind runtime, por regla del proyecto) — el proceso de build (`CONVENCIONES_TESO_1.md`) nunca se había corrido para esas 2 clases arbitrarias, así que el tamaño de fuente del encabezado probablemente NUNCA se aplicó correctamente desde que se escribió, ni antes de esta sesión. Corregido corriendo el build real (`cd C:\Users\Victor\veva-tailwind-build && npx tailwindcss -i ./input.css -o ./dist/tailwind.css --minify`, que escanea TODO el proyecto) y reemplazando `Styles.html` completo con el resultado. `revisor` (ronda 2) y `qa` verificaron independientemente que las clases nuevas están compiladas, que el rebuild no perdió ni corrompió clases usadas por las otras 14 vistas del proyecto, y que la lógica de `celdaHead` es correcta. Pusheado y verificado byte a byte. Desplegado a producción en ambos IDs (`@46`/`@47`). |
| TESO Tesorería 360 — Rediseño fila "Aplicado" (grid propio de 10 columnas) + candado de TC al aplicar | TERMINADO | Usuario reportó que la celda "Autorización" compartida con Banca amontonaba 4 controles (cuenta/moneda-real/TC/≈MXN) en ~110px, y que seleccionar "MXN (acuerdo)" sin TC parecía guardarse pero no se persistía server-side. Proceso: mockup iterado 4 rondas con el usuario (Artifact) hasta aprobación ("ok"), luego `planificador` (toca funciones compartidas por las 5 pestañas de Pagos) recomendó extraer una función dedicada en vez de tocar la compartida — implementado así. Cambios: `COL_HEAD_STYLE_APLICADO` (10 columnas propias, separadas de `COL_HEAD_STYLE_RESOLVER` de Banca que queda intacto), `renderColHead`/`colHeadStyle` con rama dedicada para 'aplicado', nueva función `renderLineRowAplicado` (Cuenta/Se paga en/TC/≈MXN como 4 celdas reales en vez de un bloque apretado; celdas vacías-pero-presentes cuando `moneda==='MXN'` para no desalinear el grid), checkbox "Aplicar" recuperado (se había perdido en un borrador del mockup, detectado por el usuario y corregido antes de implementar). Candado de TC: bloquea el lote completo al dar clic en "Aplicar" (no de forma permanente al cargar) si alguna línea marcada tiene `monedaPagoReal==='MXN'` sin `tcAplicado` — implementado en 2 capas, frontend (`resolverAplicado`) y backend (`_aplicarPagoParcialCore`, respaldo server-side ya que es el núcleo real compartido por `aplicarPagoParcial` individual y `bulkResolverAplicado`, así la regla no se puede saltar llamando al backend directo). Botones renombrados: "Resolver seleccionadas" → "Aplicar" (Aplicado) / "Subir" (Banca). `revisor` aprobó en 1er intento (2 notas menores no bloqueantes: falta `data-focus-key` en los inputs nuevos, y tamaño de la función nueva — ninguna ameritó rechazo). `qa` confirmó LISTO con 7 casos (conteo de columnas, celdas vacías válidas para MXN nativo, bloqueo de lote completo frontend+backend, checkboxes mutuamente excluyentes intactos, botones renombrados, cero regresión en Banca/Tesorería/Liberación2/Contabilidad). Desplegado y verificado contra el servidor real: `clasp push --force` (32 archivos; se detectó y corrigió un duplicado accidental `Svc_Pagos.js` en la raíz de `teso-push-local` antes del push final, dejando solo `backend/Svc_Pagos.js` como corresponde) + `clasp pull` a directorio temporal + `diff -rq` sin diferencias + `clasp deploy -i` a ambos IDs de producción (`AKfycbwYxMVPHDWthysYPhnZ_jLccJXxw3VcLDK6iLf-sLfuMX4HGMoKC9xzx6C5ymqL7IOX_A` @48, `AKfycbzpHYuzbGNxYK3_bv3OgUZh5QnLugJcpp9pIht2GbGN7wcHFM4XIE-XqKgwrqCAVv7FfQ` @49). |
| TESO Tesorería 360 — Rebranding "VEVA Enterprise" → "Ventura Entertainment" en el correo de Posición Bancaria (Lista de Distribución) | TERMINADO (parcial — ver pendiente) | Usuario preguntó 3 cosas sobre `enviarReportePosicion` (Codigo.js): (1) qué tan complejo sería que el reporte tuviera drill-down por sociedad en las clasificaciones que no son "Operativas" — **ya estaba implementado**, el código agrupa por `tipo` (`TYPE_ORDER = ['Operativas','JV','No Operativas','Restringidas','Otras']`) y despliega cada sociedad dentro de cada grupo sin distinción especial para Operativas; la captura que compartió el usuario es exactamente esa salida ya vigente, cero trabajo necesario. (2) Rebranding de texto "VEVA Enterprise"→"Ventura Entertainment" en el correo — hecho, 3 lugares dentro de esa única función (header HTML, pie de página, subject), acotado a esa función a propósito (no se tocó "VEVA Enterprise" en `Index.html` ni en comentarios de otros archivos backend, fuera del alcance pedido). `revisor` aprobó en 1er intento. `qa` confirmó `LISTO`. Pusheado y verificado byte a byte contra el servidor. Desplegado a producción (`@50`/`@51`, ambos IDs). (3) **Pendiente, sin implementar a propósito**: cambiar el remitente a `tesoreria@venturae.com.mx` requiere pasar de `MailApp` a `GmailApp` con `from` explícito — pero eso solo funciona si esa dirección ya está configurada como alias "Enviar correo como" en la cuenta de Gmail de quien ejecuta el envío (la app corre `executeAs: USER_ACCESSING`, no como una cuenta fija), y sería el primer uso de `GmailApp` en el backend (reautorización de permisos la primera vez). El usuario no supo confirmar si el alias ya existe — pausado hasta que lo confirme con quien administra Workspace, para no romper el envío de un reporte financiero real en producción. |
| TESO Tesorería 360 — Bug real: no se podía subir propuesta con 2 hojas/sociedades + sociedad "Acuario de Bajío" no reconocida + `[object HTMLSpanElement]` en Histórico de Pagos | TERMINADO (con 1 pendiente de dato, ver nota) | Usuario reportó no poder subir `Propuesta de pago Acuarios - VCP (1).xlsx` (2 hojas de Excel, "BL"=Blau Life SAPI de CV y "AB"=Acuario de Bajío SA de CV) y el error `[object HTMLSpanElement]` en la columna Sociedad de Histórico de Pagos. Diagnosticado leyendo el archivo real con `openpyxl` y el código real (no especulación) — 3 bugs confirmados. **Bug 1**: `previsualizarPropuestaPago` (`Svc_Pagos.js`) solo procesaba la PRIMERA hoja válida del Excel y descartaba el resto en silencio — con el archivo real, la hoja "AB" desaparecía sin ningún aviso. Fix: procesa TODAS las hojas candidatas, fusiona resultados; si a una hoja le faltan columnas requeridas se reporta como error de esa hoja sin abortar las demás (decisión explícita del usuario). Respuesta cambió de `data.hoja` (string) a `data.hojas` (array); frontend (`View_Pagos.html`) actualizado para consumirlo, con hoja de origen visible en cada error de fila. **Bug 2**: `_resolverSociedadDesdeNombreEmpresa` no normalizaba acentos ("Acuario de Bajío" con acento vs. dato bancario real sin acento, confirmado contra `cargasaldos/30_07_2026...csv`: "ACUARIO DEL BAJIO SA DE CV"). El usuario, tras preguntas dirigidas, decidió un rediseño más de fondo: agregar una 3ra estrategia de resolución PRINCIPAL — si "Nombre empresa" es puramente numérico, se interpreta como `ID_SOCIEDAD` del catálogo (4 dígitos, identificador de la sociedad, distinto de `ULTIMOS_DIGITOS` que es de una cuenta) — el nombre de texto libre (ahora con acentos normalizados) queda como respaldo, no se rompe nada de lo que ya funcionaba. Las 3 estrategias son mutuamente excluyentes por construcción (verificado por `revisor`). **Pendiente real, documentado a propósito, no resuelto por diseño**: la discrepancia "DEL" (banco/catálogo) vs "DE" (Excel) para "Acuario de Bajío" sigue sin resolver — es una diferencia real de dato, no de formato, y no se corrige con fuzzy-matching automático (arriesgaría mezclar sociedades reales distintas). El usuario puede desbloquear esto ahora mismo migrando esa fila del Excel (o de aquí en adelante todas) a usar el número `ID_SOCIEDAD` en vez de texto libre — la forma recomendada hacia adelante. **Bug 3**: `td()` en `View_HistoricoPagos.html` forzaba `String()` sobre un nodo DOM (el badge de Sociedad), produciendo literalmente `[object HTMLSpanElement]` — fix de una función, mismo criterio que su función hermana `tdNum()`, ya usaba correctamente. Se confirmó que `f.sociedad`/el filtro de Sociedad en ese reporte ya usaban consistentemente el mismo "nombre corto" (no hacía falta ningún cambio adicional de datos ahí). `analista`→`planificador` (mandatorio por tocar lógica de negocio en 2+ archivos) con 2 decisiones abiertas resueltas por el usuario vía preguntas directas antes de codificar (hoja incompleta no aborta el archivo completo; ID_SOCIEDAD es forma principal, nombre libre queda de respaldo). `revisor` aprobó en 1er intento con 2 notas menores no bloqueantes (mensaje de resumen vacío si 0 hojas procesables, y falta de chequeo de UUID duplicado DENTRO del mismo lote) — ambas corregidas antes de `qa` por ser baratas y directamente relevantes al cambio recién hecho. `qa` confirmó `LISTO` con 7 casos, incluyendo el archivo real del usuario releído con `openpyxl`. Pusheado y verificado byte a byte contra el servidor (32 archivos, `clasp pull` + diff sin diferencias). Desplegado a producción (`@52`/`@53`, ambos IDs). |
| TESO Tesorería 360 — Control de acceso por VISTA y por SOCIEDAD (Pagos/reportes) — en cola | NO_INICIADO (encolado 2026-08-02, pendiente de retomar) | Pedido del usuario: "Para el comité considerar el control de acceso por vista y en el caso de pagos o reportes por sociedad, cual es la mejor forma para poder realizar esto cumpliendo los lineamientos de control de los md antes de codificar." Surgió mientras se resolvía el bug/UX de "CC de Pagos" (ver fila de arriba) — el usuario aclaró que es una necesidad aparte, no una continuación de ese tema, y pidió encolarla en vez de interrumpir lo que estaba en curso. Estado real del código (confirmado, no supuesto): `CAT_USUARIOS` hoy es `EMAIL/NOMBRE/ROL/ACTIVO` — el rol da acceso GLOBAL, sin ningún concepto de "sociedades permitidas" ni de "vistas permitidas" por usuario; `_tieneAccesoValido` falla ABIERTO si el catálogo está vacío. Pendiente relacionado ya documentado (ronda 2026-07-28): "matriz de acceso admin de solo lectura" — decidida en su momento como reporte, no como control real; falta confirmar si este nuevo pedido la reemplaza o convive con ella. Preguntas abiertas identificadas antes de planificar (a resolver con el usuario cuando se retome, `analista` primero por ser una tarea grande/ambigua que toca seguridad): (1) "control por vista" ¿es ocultar del menú, bloqueo real server-side, o ambos? (2) "reportes" ¿cuáles vistas exactas entran (Histórico de Pagos, Posición Bancaria, Flujo de Efectivo, Conciliación, Cobranza)? (3) restricción por sociedad ¿solo lectura/visibilidad, o también bloquea acciones (aprobar/liberar/aplicar)? (4) ¿dónde vive el dato de permisos (columna nueva en CAT_USUARIOS, hoja nueva tipo CONFIG_CC_PAGOS por usuario+sociedad, derivado del rol)? (5) comportamiento por defecto si un usuario no tiene sociedades configuradas: ¿falla abierto (ve todo, consistente con el criterio actual) o cerrado (no ve nada)? (6) ¿se necesita UI de administración nueva, o basta con editar la hoja directo? **Retomar con `analista` antes de `planificador`.** |
| TESO Tesorería 360 — CC de Pagos: modo "Por persona" (ver/editar un email a través de todas las sociedades) | TERMINADO | Usuario reportó que desactivó a alguien en "CC de Pagos" pensando que dejaría de recibir copias, pero seguía activo — confirmado NO era bug: `CONFIG_CC_PAGOS` tiene una fila independiente por combinación email+sociedad, y la UI solo mostraba una sociedad a la vez (dropdown), sin forma de ver a una persona a través de todas. Alcance acotado explícitamente por el usuario a esto (descartó, por ahora, tocar el control de acceso real — ver fila de abajo, en cola). `planificador` confirmó: 100% frontend (`View_ListaDist.html`), `state.ccListaCompleta` ya trae todos los datos en memoria, sin cambios a `Svc_Pagos.js`. Se agregó toggle "Por sociedad / Por persona"; en modo Persona, buscar un email (input con `<datalist>` de sugerencias) muestra una tabla con una fila por CADA sociedad del catálogo, con su estado real o "Sin copia configurada aquí" si no tiene fila ahí. Decisión de producto confirmada con el usuario vía pregunta directa con ejemplo concreto: activar el toggle en una sociedad donde la persona no tiene fila CREA la fila automáticamente (un clic, sin pasar por el formulario de alta) en vez de dejar el toggle deshabilitado. `revisor` RECHAZÓ en 1er intento (2 hallazgos: switch/toggle duplicado literal entre `ccRenderFilaDestinatario` y la función nueva — extraído a helper compartido `ccRenderToggle`; inconsistencia de opacidad `opacity-60` vs `opacity-50` ya usado en el resto del archivo) — corregido y aprobado en 2do intento. `qa` confirmó `LISTO` con 7 casos (incluyendo creación silenciosa de fila nueva sin tocar otras sociedades, búsqueda case-insensitive, gateo de permisos consistente, cero regresión en modo sociedad). Pusheado y verificado byte a byte. Desplegado a producción (`@54`/`@55`, ambos IDs). **Nota de proceso**: mientras se revisaba esta tarea, el usuario pidió otra cosa distinta (control de acceso por vista/sociedad) y explícitamente corrigió que debía encolarse en vez de interrumpir — ver fila de abajo. |
| TESO Tesorería 360 — Hallazgo operativo grave: el usuario estaba usando proyectos de Apps Script equivocados (2 copias distintas, no el proyecto real que se mantiene esta sesión) | TERMINADO (diagnóstico) | Usuario reportó que "en el sistema" la Posición Bancaria mostraba el 10 de agosto pero en `HIST_POSICION_BANCARIA` (revisado a mano) solo hasta el 7 — investigado sin especular: `getBankingPosition` lee EXCLUSIVAMENTE de esa hoja, sin caché ni fuente alterna (confirmado por código), así que la contradicción solo podía explicarse por el usuario mirando un proyecto distinto. Se identificaron 3 proyectos de Apps Script reales, confirmados uno por uno vía capturas de pantalla y `clasp deployments`/`.clasp.json`: **(A) el correcto** — `1lqYDW6RW8SxuVQtwS6cvNwA1BuxePKoimfl9GCk4CJOGRL6daoJmjwOc`, el que se mantiene toda la sesión, con `SALDOS_SHEET_ID = 1-DzPpYYViWVG0RHuGGldkDo4LUyYcCLAjQOIlp2DdS8` (BD_TESO real); **(B)** `1SS-etE87qvUHrpC70baZfACAk-F9Q41rQWW5K8cg3jQJkQG3WjgW1wed`, creado por el usuario el mismo día ("Versión 1") porque GAS le mostraba pantallas vacías bajo un despliegue viejo y en vez de reportarlo creó un proyecto nuevo; **(C)** `18e-3X_yVVMOZsGmi08pafmACkb4Hpg3cz_e25pKfqkGgaPwDzCje09hJ`, el sistema "VEVA" original pre-migración (código dice literalmente "VEVA Enterprise v0.3.0", archivos `.gs`, versión 67 de mayo 2026), con un `SALDOS_SHEET_ID` que empieza `10BFY...` — **confirmado que es una hoja de cálculo distinta y abandonada**, no `BD_TESO`. Riesgo real: si el usuario cargó saldos bancarios reales a través del proyecto C, esos datos NO llegaron a la base real y hay que re-cargarlos. **Pendiente**: revisar (solo lectura) la hoja vieja del proyecto C para confirmar si la carga del 10 de agosto quedó ahí perdida — el usuario no ha confirmado todavía si procede. **Lección de proceso, ya aplicada**: cuando algo se ve vacío/roto en GAS, la reacción correcta es reportarlo para diagnosticarlo, nunca crear una implementación o proyecto nuevo — se le explicó esto directamente al usuario. Los 2 enlaces `/exec` reales y verificados (usar siempre estos, ignorar cualquier otro): `AKfycbwYxMVPHDWthysYPhnZ_jLccJXxw3VcLDK6iLf-sLfuMX4HGMoKC9xzx6C5ymqL7IOX_A` y `AKfycbzpHYuzbGNxYK3_bv3OgUZh5QnLugJcpp9pIht2GbGN7wcHFM4XIE-XqKgwrqCAVv7FfQ`. |
| TESO Tesorería 360 — Rebranding "VEVA Enterprise" → "Tesorería VLMM" (título/encabezado de la app) + remitente real de los 3 correos del sistema | TERMINADO (con 1 pendiente explícito) | Pedido del usuario, en 2 partes. (1) Rebranding: reemplazado en `Index_1.html` (`<title>` y header visible) — se descubrió de paso que `TESO_src\Index.html` es una copia vieja/no usada (`diff` contra el desplegado real no coincidía) y que `Index_1.html` (raíz del proyecto) es la fuente real — documentado para no repetir el error. Alcance acotado a título+encabezado, sin tocar comentarios de código, por decisión explícita del usuario. (2) Remitente: `MailApp` → `GmailApp` con `from` explícito en los 3 correos reales del sistema (`enviarReportePosicion` en `Codigo.js`, y `_enviarCorreoNotificacionPagoSeccionado`/`_enviarCorreoNotificacionPagoUnica` en `Svc_Pagos.js`) — usando `tesoreria@dolphinaris.com` (alias "Enviar correo como" ya confirmado por el usuario), con `name: 'Tesorería VLMM'`. **Pendiente explícito, documentado en comentarios en el propio código**: migrar a `tesoreria@venturae.com.mx` cuando el usuario confirme "actualizar correo" — ese alias no estaba confirmado al momento de este cambio. Es la primera vez que el backend usa `GmailApp` (antes solo `MailApp`) — posible pantalla de reautorización de permisos la primera vez que alguien dispare estas funciones tras el despliegue. `revisor` aprobó en 1er intento (comparó byte a byte contra lo ya desplegado, confirmó `cc: ... : undefined` es equivalente al `if` anterior, confirmó try/catch de aislamiento de fallas intactos). `qa` confirmó `LISTO` con 6 casos. Pusheado y verificado byte a byte. Desplegado a producción (`@56`/`@57`, ambos IDs). |
| TESO Tesorería 360 — Correo de tesorería corporativa configurable + remitente condicional por tipo de transición de pago | TERMINADO | Refinamiento del cambio anterior (fila de arriba): el usuario aclaró que el remitente fijo `tesoreria@dolphinaris.com` no debía aplicar a TODOS los correos — solo a Posición Bancaria y a la notificación de pagos cuando la transición es AUTORIZADA; las otras 6 transiciones (CARGADA/CONFIRMADA/LIBERADA A BANCO/SUBIDA EN BANCA/APLICADA EN BANCA/RECHAZADA) deben salir de la cuenta real de quien ejecuta esa acción, para trazabilidad ("quien carga, sube o aplica, ahí ya es la persona que lo hace"). Además pidió que el correo de tesorería ya no esté fijo en código sino editable desde una pantalla, con control de quién puede editarlo. Verificado con el usuario (confirmó explícitamente) que `AUTORIZADA` nunca se agrupa con otra transición distinta en el mismo correo "seccionado" — los únicos casos de correo con 2 secciones son variantes positivo/negativo de LA MISMA acción en lote (aplicado+rechazado, subido+rechazado), nunca autorizado mezclado — así que la decisión de remitente nunca es ambigua dentro de un mismo correo. **Backend** (`Svc_Pagos.js`): hoja nueva `CONFIG_TESORERIA` (mismo patrón CLAVE/VALOR/DESCRIPCION que `CONFIG_CONCILIACION` ya existente) con `EMAIL_TESORERIA` (semilla `tesoreria@dolphinaris.com`) y `ADMINS_TESORERIA` (semilla `vaviles@venturae.com.mx`, lista de emails separados por coma) — esta lista de administradores es APARTE del sistema de roles `CAT_USUARIOS` por decisión explícita del usuario (más sensible que un rol operativo: controla de dónde salen las comunicaciones financieras reales). Funciones: `_leerConfigTesoreria`/`_esAdminTesoreria`/`_remitenteParaTransicion` (helper compartido que decide `from`/`name` o `null` según el tipo de transición) y `getConfigTesoreria`/`guardarConfigTesoreria` (expuestas al frontend, escritura rechazada server-side si no es admin, nunca permite guardar con 0 administradores). **Frontend**: pestaña nueva "Remitente" en Lista de Distribución (`View_ListaDist.html`) — ver/editar el correo y la lista de administradores (chips con quitar/agregar), con aviso explícito de que el correo configurado debe ser un alias "Enviar correo como" ya verificado en Gmail (el sistema no puede validarlo de antemano). `revisor` aprobó en 1er intento con 1 observación no bloqueante (si alguien borra a mano la fila CLAVE de la hoja, el guardado fallaría en silencio) — corregida antes de `qa` (ahora usa `appendRow` para reinsertar la fila en vez de saltarse el guardado). `qa` confirmó `LISTO` con 10 casos. Pusheado y verificado byte a byte. Desplegado a producción (`@58`/`@59`, ambos IDs). Con esto queda cerrado, como configuración editable en vez de código, el pendiente de la fila anterior sobre migrar a `tesoreria@venturae.com.mx` — ya no requiere ningún cambio de código, solo editar el campo cuando se decida. |
